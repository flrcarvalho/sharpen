# Pisos mínimos + tetos de major/minor para evitar que um rebuild no Railway
# (deploy automático em push) puxe uma versão maior e quebre em produção sem
# mudança de código. Um lockfile (pip freeze da imagem / uv lock) é o próximo passo.
fastapi>=0.115.0,<0.116.0
uvicorn[standard]>=0.30.0,<1.0.0
anthropic>=0.49.0,<1.0.0
python-multipart>=0.0.9,<0.1.0
asyncpg>=0.30.0,<0.31.0
xlrd>=2.0.1,<3.0.0
httpx>=0.27.0,<1.0.0
bcrypt>=4.1.0,<5.0.0
