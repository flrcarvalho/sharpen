# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/

# Variáveis de ambiente
.env
.env.*
!.env.example

# Backups (gerenciados manualmente — não entram no git)
Backups/

# Node — dependencia de FERRAMENTA, nao do app (o deploy e' Python puro).
# Nasce do `npm install puppeteer-core`, usado so por scripts/demo/capturar.mjs.
node_modules/

# OS
.DS_Store
Thumbs.db
_backups/

# Claude Code — permissões locais (não versionar); commands/ é versionado
.claude/settings.local.json
