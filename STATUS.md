# STATUS — Masters & Casas (FDC Capital / Planilhador)

Documento de rehydration de sessão. Quem abrir o Claude Code neste repo lê isto primeiro.

> ⚠️ **STATUS ≠ fonte de regras.** Este arquivo é um **changelog/rehydration** (o que mudou e por quê). As regras vinculantes vivem nos **`global/MASTER_*`** (domínio de apostas), em **`pack/tokens/tokens.css`** + **`pack/CLAUDE.md`** (marca/design) e nos **`CLAUDE.md`** (operacional). Não decida uma regra consultando o STATUS — siga o ponteiro para o canônico.

Repo local: `C:\Users\Fernando\Downloads\FDC Capital\Planilhador`

_Atualizado: 2026-07-26 (sessão 199 — **Betfair: a paginação ativa NUNCA funcionou — o contexto do replay ficava preso na aba errada.** **Os 3 sintomas do Feca na 0.6.27, todos da MESMA raiz:** (a) capturando pela aba Aberta, saíram só os 2 bilhetes abertos e nenhuma encerrada; (b) pela aba Resolvida, saíram só os **22 visíveis** — ele pediu **100**; (c) "não rola a tela até o fim para carregar mais". **Cadeia causal (provada por mutação no harness, não deduzida):** o `capturarReq` guardava a **1ª requisição com body** e a página abre na aba "Aberta" → o contexto do replay ficava preso em `"status":"OPEN"` → o `paginarLoop` repaginava a lista ERRADA → o `forward` recusava a resposta (não é SETTLED) → `break` na 1ª volta → e o **`finally` marcava `fimReal = true`**. No `content.js` o laço de rolagem é `while (… && !bfFimReal)`: **o robô nunca chegava a rolar**. Não era o scroll que falhava — era o `fimReal` mentindo antes de ele começar. **Fix:** cada aba tem o seu contexto (`reqOpen`/`reqSettled`, separados pelo `"status"` do próprio corpo) e o `paginarLoop` usa o da RESOLVIDA; o antigo `reqCtx` foi **removido** (era a origem do bug). **Um clique cobre as duas listas venha de onde vier:** `reqDe(status)` prefere o contexto real e, quando a página não disparou aquela aba, **deriva** o corpo da outra (troca `status`, zera o índice, ajusta `dateFilter` — 0 na Aberta, 90 na Resolvida); novo `buscarEncerradas()` puxa a 1ª página quando a captura começa pela aba Aberta, e o `forward` encadeia a paginação a partir dela. **Bug adjacente corrigido:** o `bodyProxPagina` trocava QUALQUER número igual ao `rangeStart` — na 2ª página `pageSize:10` == `rangeStart:10`, então **o tamanho da página era sobrescrito pelo índice**; agora só mexe em chave com nome de índice (`_IDX_RE`) e nunca em `size/limit/count/length`. **Inatividade:** o inject reporta `paginando` e o robô conta isso como sinal de vida — antes ele desistia após 12s parado no meio da paginação, encurtando o lote. **Harness ganhou músculo:** o `sandbox` passa `opts` (method/body) ao `responder` e aceita `corpoInicial` — sem isso é impossível distinguir duas requisições que compartilham a MESMA URL e só diferem pelo corpo, que é o caso da Betfair. O caso novo simula o servidor por `status`+índice e roda **um clique partindo de CADA aba**, exigindo 20 encerradas (2 páginas), 2 abertas, `fim` sinalizado, `fromRecord:10` pedido e `pageSize` intacto. **Mutação (bug antigo reintroduzido) reproduz os 3 sintomas do Feca à risca:** `aba Resolvida: 0 abertas` · `aba Aberta: 0 encerradas` · `nenhuma requisição pediu a 2ª página`. Manifest **0.6.27→0.6.28**. Gates: harness **3 casos / 65 bilhetes verde** · `audit_sharpenup` sem FAILs · **211 passed, 4 skipped** · `node --check` OK. Backup `Backups/betfair-paginacao-duas-abas/`. **NÃO validado ao vivo** — o Feca recarrega (0.6.28) + Ctrl+Shift+R e clica "Copiar bilhetes" UMA vez, de qualquer aba (esperado: até o teto pedido em encerradas + as abertas no mesmo lote). **Confirmado pelo Feca e já era o comportamento do código:** em W a odd é SEMPRE Retorno÷Stake, jamais a exibida; em L/V vale a exibida. **PENDENTE:** o `CASA_BETFAIR §6` diz o contrário ("a odd exibida é autoritativa para W") e contradiz o §11 do mesmo arquivo — diff proposto, aguardando aval (invariante 1). Ver [[betfair-data-mes-com-ponto]]. **Anterior: s198 abaixo.**)_

_Anterior: 2026-07-26 (sessão 198 — **"extraí na Tivo e não veio nada": os 22 bilhetes ESTAVAM no banco — numa conta que não existe mais.** **Sintoma:** card "✓ 22 novo(s)", RAIO-X "22 aposta(s) na grade · 100% confiança" e a grade em **0 apostas**, KPIs zerados, histórico da conta vazio. **Prova (SELECT em produção, não dedução):** `bilhetes` com `casa='Tivo', parceiro='Feca [[Eu]]', dono='Feca', n=22` — e **nenhuma linha correspondente em `parceiros`** (a conta real é `parceiros#351 = Tivo / Feca [Eu]`, criada 11:28:23, 1min antes do lote). **Bilhete órfão:** a grade filtra por `casa + parceiro` em TEXTO, então as 22 linhas existiam e nenhuma tela as alcançava. **Causa raiz (confirmada pelo Feca):** a conta nasceu `Feca [[Eu]]` (o campo *Fornecedor* do modal recebeu `[Eu]` **com colchetes** e o `ncCriarConta` embrulha em `[...]` de novo — `index.html:2807`) e foi **renomeada com o lote EM VOO**. A corrida: o card leva `parceiroSnapshot = {...parceiroSelecionado}` (cópia **rasa**, nome congelado no clique, `index.html:4189`); o `renomear_parceiro` propaga o nome novo aos bilhetes **que já existem** (`repository.py:2069`) — nenhum do lote naquele instante; minutos depois o `/salvar` grava com o nome **velho** do snapshot; o `carregarGrade` consulta pelo nome **novo** → 0 linhas. **Ninguém comparava "salvei 22" com "a grade voltou 0"** → sumiço silencioso, mesma família do incidente `parse_tsv`/Betfair (s193). **(1) Reparo — APLICADO:** `scripts/reparar_orfaos_parceiro.py` (novo, genérico: sem argumento = relatório de todo `(dono,casa,parceiro)` sem conta correspondente). **Recalcular a assinatura é obrigatório, não detalhe:** `_assinatura` inclui o `parceiro` no hash (com ou sem código), então mover a conta sem recalcular faria a próxima captura da Tivo **duplicar o lote inteiro**. 21 movidos; 1 recusado pelo próprio guard de colisão — e ele estava **certo**: às 15:02 o Feca re-extraiu e o bilhete aberto `298782220` já existia na conta certa (a 2ª extração trouxe só 2 bilhetes porque o pré-dedup por código pula os já resolvidos **do dono**, não da conta). Decisão do Feca: ficar com a descrição completa da 1ª extração (`… Under 3,5 Escanteios **Time de Fora**`) — copiada para o `#72167` e o órfão `#72165` apagado. **Conta `Tivo · Feca [Eu]` fecha com 23 bilhetes** (os 22 + o `291115493`, que na 1ª captura veio como "Result undefined" sem seleções e na 2ª veio completo). Backup dos 22 em `Backups/s195-orfaos-parceiro/*.csv`. Sobra 1 órfão antigo **fora deste escopo**: `Jonathan / Faz1bet / 'Pessoal'` (1 bilhete, 04/07). **(2) Raiz — o `/salvar` deixa de confiar no texto do cliente:** novo `get_parceiro(id, dono)` (`repository.py`) + campo `parceiro_id` no `SalvarRequest`; quando ele vem, `casa`/`nome` são lidos do **banco no instante da gravação** e mandam sobre o texto. Busca no **dono efetivo** (é por ele que as contas são listadas em "ver como"), gravação segue no **dono real**. Sem `parceiro_id` ou sem match → comportamento antigo intacto (extensão, import, Polymarket, `/bilhetes/manual`). O front passa `parceiro_id: parceiro.id` no POST. **(3) Guard — a contradição nunca mais passa calada:** o `/salvar` devolve a `casa`/`parceiro` **efetivamente gravados**; depois do `carregarGrade()`, `salvou N` + `grade 0` vira card **vermelho** ("N salvo(s) em «X» — não aparecem na conta ativa «Y»"), e rename detectado durante o lote vira aviso âmbar informativo (o servidor já corrigiu pelo ID). Pega esta causa **e qualquer divergência futura**. **Gates:** `202 passed, 4 skipped` (+3 novos em `tests/test_salvar_parceiro_id.py`, regressão direta deste incidente) · `audit_casas` e `audit_sharpenup` sem FAILs · harness 3 casos/59 bilhetes verde · sintaxe dos 3 blocos JS inline do `index.html` OK. Backup `Backups/s195-parceiro-id-guard/`. **(4) Bug irmão, achado no caminho e CORRIGIDO:** `renomear_parceiro` movia os bilhetes mas **não recalculava a assinatura** — e o `parceiro` entra no hash, então **toda conta renomeada duplicava o histórico na próxima captura** (mesma família do `_assinatura_pos_edicao`, s145, que cobriu a edição de bilhete e deixou o rename de fora). Agora o rename lê os bilhetes ANTES, troca o nome e recalcula a assinatura de cada um **depois** (a checagem de colisão do `_assinatura_pos_edicao` precisa enxergar as irmãs já na conta nova); duas linhas de conteúdo idêntico escalam o `_counter`, como o UPSERT já faz. `tests/test_renomear_parceiro_assinatura.py` (4 casos, sem Postgres) + **teste de mutação**: desligar o recálculo derruba 3 dos 4. **Fica ABERTO o backfill** — contas renomeadas ANTES deste fix seguem com o hash velho no banco. **(5) Tivo: bilhete "esqueleto" apagava bilhete real — CORRIGIDO.** Os 3 blocos que subiram vazios no lote de 26/07 (`291115424` · `291115493` · `291115639` — "Status=undefined · Result=undefined" e `Seleções:` em branco) foram **12% do lote**. **Não é dado que falta na casa:** os 3 vêm CHEIOS na fixture do recon (`Status:10 · Result:3 · Items:3`). O mecanismo: `parseTicket` aceita qualquer objeto com `ID`, e o `guardar` só troca quando `aberto(existente) && !aberto(novo)` — um esqueleto tem `status: undefined`, logo **não conta como aberto**: chegando primeiro, ele nunca era substituído pelo bilhete cheio. Primeiro a chegar ganhava, e o vazio se disfarçava de resolvido. **Fix nos dois lados** (o `tv_inject` e o ouvinte `tvById` do `content.js` repetem a mesma regra): **conteúdo vence esqueleto, sempre**. E esqueleto que sobrar sai **NOMEADO** (`SEM DETALHE — a casa devolveu só o identificador`), mantendo o `[Código:]` para a conferência de cobertura cobrar o bilhete e mandar recapturar, em vez de virar bloco mudo que a IA tentava adivinhar; `CASA_TIVO §5.1` manda **não gerar linha**. Harness: 3 conferências novas (esqueleto ANTES e DEPOIS do cheio na mesma resposta + esqueleto sozinho), **25 bilhetes** no caso Tivo, e **teste de mutação** — tirar o fix derruba com "o esqueleto venceu o bilhete cheio". Manifest **0.6.26→0.6.27**. **A ORIGEM do esqueleto segue sem prova:** qual mensagem da API devolve `Tickets` só com `ID` só se descobre com o F12 aberto na casa; o fix não depende disso (é defensivo), mas fica anotado. **(6) Pré-dedup por código passa a enxergar a CONTA — CORRIGIDO.** Foi o que fez a 2ª extração da Tivo trazer **2 de 25**: os outros 20 estavam no banco como `resolvida` **do mesmo dono** — na conta órfã — e eram descartados **antes de chegar na IA**; repetir a extração não adiantava nada, o lote nunca apareceria. O UPSERT trata conta como espaço separado (`UNIQUE (dono, casa, parceiro, assinatura)`), mas `get_codigos_resolvidos`/`get_codigos_existentes` filtravam **só por `dono`** — dois escopos diferentes para a mesma pergunta. Agora `casa`/`parceiro` entram como filtro **opcional** (`_filtro_conta`, placeholders numerados dinamicamente) e os 3 chamadores do `/extrair` passam a conta. **Medido antes de mexer:** hoje há **zero** código repetido em mais de uma conta do mesmo dono (query em produção) → o fix não muda nada retroativo, só fecha o modo de falha. Nome de conta velho no formulário degrada para "não pula nada" (a IA reprocessa e o UPSERT dedupa), nunca para "esconde bilhete". +5 testes de placeholder/fallback e **smoke no Postgres real**: conta certa acha, conta errada devolve vazio, e o bilhete ABERTO (`298782220`) segue fora do descarte. **(7) A origem fechada:** o modal "+Nova conta" agora **saneia colchetes** no campo Fornecedor (`replace(/[\[\]]/g,'')`) — os colchetes são do MODELO `Parceiro [Fornecedor]`, não do que se digita. Digitar `[Eu]` criava `Feca [[Eu]]`, obrigava a renomear, e foi um rename desses (com lote em voo) que gerou os 22 órfãos. **PENDÊNCIAS honestas:** (1) **Não validado ao vivo** — o Feca recarrega `/app#plan` na conta `Tivo · Feca [Eu]` (esperado: 23 linhas, KPIs preenchidos, o `298782220` **aberto**). Ver [[dedup_gap_sem_codigo_reextracao]] · [[assinatura_stale_edicao]] · [[casa_modo_cego_nome_verbatim]]. **Anterior: s197 abaixo.**)_

_Anterior: 2026-07-26 (sessão 197 — **Betfair: abertas + encerradas num clique só, e o cashout deixa de virar W.** **Pedido do Feca:** "quando apertar extrair na Betfair, trazer liquidadas + abertas de uma vez, já que estão na mesma página em abas diferentes". **Desenho ADITIVO (não encosta no caminho que acabou de voltar a funcionar):** o `forward()` do `bf_inject` já devolvia `null` para a resposta `status:"OPEN"` — agora ela vai para um **balde separado** (`abertas[]`) e a função **segue devolvendo `null`**. `respostas`, `all`, `fimReal`, `ultimoNext` e o `paginarLoop` continuam alimentados SÓ pela lista SETTLED → a captura das encerradas fica byte a byte igual; o harness trava isso (`resposta OPEN vazou para 'bets'`). **Um clique basta:** a página dispara a requisição da lista Aberta sozinha ao carregar (provado no console do Feca), e a extensão só a descartava; como rede de segurança, o inject guarda essa requisição à parte (`reqOpen`, sem tocar no `reqCtx`) e a refaz uma vez se o balde estiver vazio — o operador **não precisa clicar na aba "Aberta"**. **Aberta (§1.1):** sobe sem resultado, com `Retorno POTENCIAL (ainda não realizado)` rotulado (sem o rótulo a IA lê retorno>0 e conclui vitória), odd = a EXIBIDA, e **Data VAZIA** — decisão do Feca, fiel ao §4.A (a coluna Data É a resolução, que ainda não existe); o UPSERT por código preenche quando liquidar. **(2) CASH OUT corrigido — o Feca confirmou o caso ("desisti da aposta, fiz o cashout, deveria entrar como Void"):** o JSON prova que o rótulo `status` é **INÚTIL** em cashout — o MESMO desfecho vem `"LOST"` no `0001821` e `"WON"` no `0001807`, ambos com Cash Out == Stake. Quem decide é o dinheiro (`MASTER_RESULTADO §5.1.2`): **V com a odd exibida**. Antes o bloco dizia "regra §7" e "Odd total: 1", e a IA gravava **W/odd 1** (o `0001821` está assim no banco). Armadilha do fix: o termo `st === "WON"` reativava `Retorno÷Stake` por fora do ramo do cashout — o harness pegou. **(3) Fixtures:** `betfair.open.json` (2 abertas reais) + `betfair.settled2.json` e `betfair.settled3.json` (2 páginas reais) — a Betfair passa de 10 para **32 bilhetes** travados, cobrindo pela 1ª vez **VOID**, **os dois cashouts**, DBL perdida com freebet e Each Way. **⚠️ DECISÃO PENDENTE (não prejulguei, está anotada na fixture):** a odd do W sai de Retorno÷Stake e carrega o ruído do arredondamento ao centavo — `0001809` vira **4,50002999** enquanto a casa exibe **4.50** (e 4,50 × 166,75 = 750,375 → 750,38, ou seja a exibida EXPLICA o retorno). Mas o `0001806` exibe 1,83 e 1,83 × 300 = 549 ≠ 550, ali o dinheiro TEM de mandar. O `CASA_BETFAIR §6` diz que na Betfair a exibida é autoritativa para W (não há boost) — o que apontaria para 4,5. A regra da KTO (s192) já resolveu isso: **a declarada vence SE explicar o retorno até o centavo, senão o dinheiro manda**. A fixture trava a regra ATUAL até o Feca decidir se estende a regra da KTO à Betfair. Manifest **0.6.25→0.6.26**. Gates: harness **3 casos / 59 bilhetes verde** · `audit_sharpenup` sem FAILs · **199 passed, 4 skipped** · `node --check` OK em todos os JS. Backup `Backups/betfair-abertas-cashout/`. **NÃO validado ao vivo** — o Feca recarrega (0.6.26) + Ctrl+Shift+R na Betfair e clica "Copiar bilhetes" uma vez (esperado: encerradas + as 2 abertas no mesmo lote; o `0001821` vira V/odd 5,5 por UPSERT). Ver [[betfair-data-mes-com-ponto]]. **Anterior: s196 abaixo.**)_

_Anterior: 2026-07-25 (sessão 196 — **Tivo entra como 8ª casa de captura — a primeira construída inteira pelo padrão da s194, e a mais barata até hoje.** **Recon (Fases 0-1, provado ao vivo na conta do Feca, só leitura):** a Tivo roda **BetConstruct sportsbook v4** num **iframe de MESMA origem** (`tivo.bet.br/sportsbookv4/`), e o histórico não tem endpoint próprio — sai de um proxy genérico do site: `POST /api/game/p/messagetosport` com `{"name":"gethistory","message":"{countOnly,language:33,from,to}"}`. **Duas descobertas que definiram o desenho:** (1) a MESMA URL serve dezenas de mensagens (saldo, notificações, tradução) → filtrar por URL não basta e filtrar pelo corpo do pedido é frágil; **quem decide é a FORMA DA RESPOSTA** (só vale o que tem `Tickets` em array); (2) **NÃO HÁ PAGINAÇÃO** — uma chamada devolve a conta inteira e a casa carimba `Count` (provado: `from` de 2020 devolve os mesmos 24 bilhetes que `from` vazio; 1 aberta + 3 ganhas + 20 perdidas = 24 ✓). Fim autoritativo = `Error:null` + `len == Count`, nunca teto de tempo. **Armadilha silenciosa confirmada:** `from`/`to` são epoch em **MILISSEGUNDOS** — em segundos a API devolve `Count: 0` com `Error: null`, ou seja **some tudo sem erro**. **Fase 2 (harness ANTES do código):** `fixtures/tivo.gethistory.json` (24 bilhetes reais, `Player` anonimizado) + `casos/tivo.mjs` com **21 bilhetes** e 6 conferências cada, todos os valores lidos do card. **A fixture achou 2 defeitos antes de existir uma linha de produção:** **(a) a TELA TRUNCA a odd** (floor, não arredonda — `208.4854`→"208.48", `16.047`→"16.04", `11.9175`→"11.91"), então o card não serve de fonte: vale o `Koef`, que é o produto exato das pernas e explica o `WinAmount` ao centavo (12,4231 × 150 = 1.863,46). **(b) a coluna "Data" do card é a COLOCAÇÃO, e o `MASTER_OUTPUT §4` quer o EVENTO mais recente** — em 2 dos 24 bilhetes isso muda o DIA (o outright de F1 é colocado 22/07 para evento em 25/07; um de tênis, 17/05 para 18/05). Eu tinha escrito o formatador só com a colocação; o harness agora trava **as duas** datas para impedir a regressão. **Fases 3-4:** `extensor/tv_inject.js` (novo) + 4 peças no `content.js` (ouvinte `tvById` com resolvida-vence-aberta · `formatTicketTV` · `roboTVPassive` · ramo no `iniciarRobo` + autodiagnóstico). **Armadilha do iframe resolvida como na Betfair/bet365:** o `gethistory` sai DE DENTRO do iframe e o `content.js` só roda no topo (`all_frames:false`) → o inject repassa a mensagem para `window.top` e o robô pede também para `window.frames[i]`; manifest com `all_frames:true`. **Outras armadilhas travadas no formatador:** `CalculatedBetAmount` é **rateio**, não stake · `Team1Score`/`Team2Score` são **estatística do mercado** (9 escanteios), não placar (o placar é `LiveScore`) · `Market.Name` vem com **placeholder** `{p1_r}` + `FinalPosition.p1` → "3º quarto" · **outright** tem `Game:null` e `Market.Name` inútil ("Free text multiwinner market"), o que vale é `Outright`+`OutrightGame` · `Result` fora de {0,2,3} sobe **cru** ("a conferir — não liquidar"). **Fase 5 (registro):** 12 pontos completos — `_CASA_DISPLAY`, `_CASAS_MARCADOR_CODIGO` (chunking + pré-dedup), `_MODO_POR_CASA`, `_HOSTS_POR_CASA`, `CASAS_CONECTAVEIS`+`NOMES`+`DOMINIOS`, `CASA_HOSTS`+dispatch do popup, manifest, `CODIGO_EXEMPLO` no audit. A **regex de código não precisou de nada**: virou genérica na s194, então casa nova entra de graça na conferência de cobertura (era exatamente a promessa). **`casas/CASA_TIVO.md`** criado com §2.5 (campos da API) e §5 (de-para `Status`+`Result`). **Gates:** harness **3 casos / 37 bilhetes verde** · `audit_sharpenup` **15/15 OK** (TIVO como robô) · `audit_casas` sem FAILs · `py_compile` OK · `node --check` OK · **199 passed, 4 skipped** · `check-tokens` OK. Manifest **0.6.24→0.6.25**. Backup `Backups/s196-tivo-sharpenup/`. **Custo: 1 sessão, contra 3 da KTO e 8 da bet365 — o padrão da s194 pagou.** **PENDÊNCIAS honestas:** (1) **NÃO validado ao vivo** — o Feca recarrega a extensão (0.6.25) + **Ctrl+Shift+R na aba da Tivo**, conecta e clica "Copiar bilhetes"; (2) **sem amostra de boost, cashout e bilhete inteiro void** — os campos existem (`CashOut`, `PossibleCashout`, `IsBonus`), mas o harness não trava o que não viu; uma perna com `Result:1` apareceu e a natureza segue **não confirmada**; (3) **3 mercados sem categoria no `MASTER_APOSTAS`** — `Total de faltas` (8 dos 24 bilhetes!), `Total de defesas do goleiro` e o rótulo de outright: decidir entre categoria nova ou `Outros` **antes do 1º lote grande**; (4) o `MASTER_APOSTAS` (tabela "unidade contada") e a `CASA_PINNACLE §9` se **contradizem** sobre handicap de sets — segui o MASTER, vale alinhar. Ver [[extensor_captura]]. **Anterior: s195 abaixo.**)_

_Anterior: 2026-07-25 (sessão 195 — **Betfair ganha regressão + conserta Each Way lido como "aposta em aberto".** **Contexto:** o Feca escolheu a fase 2 (abertas). No caminho, a Betfair virou a 2ª casa com harness (só a KTO tinha) — e a fixture achou defeito antes de qualquer linha nova. **(1) Fixture + caso (`extensor/harness/fixtures/betfair.settled.json` + `casos/betfair.mjs`):** payload REAL de 10 bilhetes colado pelo Feca do `POST /activity/sportsbook`, cobrindo W, L, múltipla DBL sem `combinedOdds`, cashout total e Each Way. **Trava a regressão da s193**: mutação removendo o `\\.?` do `_dbrBF` derruba o caso com "data esperada 18/07/2026, veio ''" — o mesmo bug que custou US$ 0,83 e 5 dias de bilhetes agora é pego offline em <1s. **(2) Each Way `PLACED` — BUG REAL corrigido:** `status:"PLACED"` **não é** aposta em aberto, é o desfecho "colocou" de um Each Way; o bilhete está liquidado (`result:"SETTLED"` + `settledDate`). O código lia o status cru → "a conferir — não liquidar" + odd 25 (a de vitória, que não ocorreu) → a IA deixava Resultado vazio e o bilhete virava `aberta`. **Eu tinha dito ao Feca que `PLACED` era a prova de que aberta vaza pelo canal SETTLED — estava ERRADO**, e implementar aquilo transformaria toda Each Way colocada em aberta eterna. Quem decide é o `CASA_BETFAIR §5` (conferência financeira): Ganhos 530 > Apostado 200 → **W**, odd = Retorno÷Stake = **2,65**. Fix em `formatTicketBF`: quando o bilhete está LIQUIDADO (`settledDate` ou `result` de liquidação) e o `status` não é WON/LOST/VOID, o dinheiro decide (`0`→L · `==stake`→V · `>stake`→W); `0 < Retorno < Stake` **não** é coberto pelo §5 → segue "a conferir", nunca chuta. Aposta genuinamente aberta não tem `settledDate` → não entra nesse ramo e segue subindo sem resultado. **Dado sujo no banco:** `O/25146258/0001772` está como `aberta`/odd 25 desde 20/07 — é um **W de R$530 sobre R$200** fora do P/L; a recaptura corrige por UPSERT. **(3) Achado ainda ABERTO (bloqueia as abertas):** a paginação ativa da Betfair está **morta**. O `capturarReq` guarda a PRIMEIRA requisição com body e a página abre na aba "Aberta" → o `reqCtx` fica preso em `{"status":"OPEN"}` (provado no console do Feca); o `paginarLoop` pagina a lista errada, o `forward()` rejeita a resposta OPEN e o loop morre na 1ª volta (harness: `urls:1`, `fim:false`). Hoje a lista só avança por **scroll passivo** — o oposto do que o comentário do `bf_inject` promete. Separar o contexto por `status` é pré-requisito das abertas. **(4) Correção de rota minha:** o dinheiro en-US (`"1,380.00"`) que eu tinha listado como regressão de locale **não é bug** — o `CASA_BETFAIR §1` documenta "números no bilhete vêm em formato en-US" desde sempre e o §10 manda normalizar. Item riscado do backlog. Manifest **0.6.23→0.6.24**. Gates: harness 2 casos/16 bilhetes verde · `audit_sharpenup` sem FAILs · **199 passed, 4 skipped**. Backup `Backups/betfair-eachway-harness/`. **PENDENTE p/ fechar a fase 2:** payload da lista OPEN (o Feca clica "Aberta" e copia a resposta) → aí entram as duas filas separadas (SETTLED que pagina + OPEN que não) e o bilhete aberto sobe sem resultado, como o `formatTicketPN` já faz. Ver [[betfair-data-mes-com-ponto]] · [[betfair_captura_json]]. **Anterior: s194 abaixo.**)_

_Anterior: 2026-07-25 (sessão 194 — **Auditoria do SharpenUp + o PADRÃO que faltava para escalar casas.** **Pedido do Feca:** "toda vez que criamos uma casa nova temos dificuldade… o que mais falta é padrão". **Medição (não impressão):** bet365 = 11 commits em 8 sessões; KTO = 3 sessões, sendo **2 desperdiçadas** (s190 registrou o backend mas o front não listava KTO em `CASAS_CONECTAVEIS` → botão morto; s191 consertou o gate; s192 descobriu que o modo estava errado desde o começo). **Causa raiz nomeada:** não existia lugar onde estivesse escrito o que uma casa precisa para existir, nem comando que provasse que ela está completa — a superfície de registro é de **12 pontos em 4 camadas**, listas paralelas que ninguém cruzava. **Entregue (nada de produção tocado):** `tools/audit_sharpenup.py` (gate determinístico dos 12 pontos; achou 2 bugs vivos sozinho) · `extensor/harness/` (regressão offline **permanente** — roda o código REAL do repo contra payloads salvos, sem navegador; a fixture da KTO foi resgatada do scratchpad antes de sumir; teste de mutação feito) · `docs/SHARPENUP_ARQUITETURA.md` (mapa, contrato de mensagens, tabela das 7 casas, os 12 pontos, dívida estrutural) · `docs/GUIA_CASA_SHARPENUP.md` (7 fases, árvore de decisão de modo, **livro de armadilhas** consolidado) · `docs/AUDITORIA_SHARPENUP_2026-07-25.md` · 4 skills (`/sharpenup-recon`, `/sharpenup-casa`, `/sharpenup-validar`, `/sharpenup-diagnostico`) · `extensor/README.md` reescrito (listava `fab.js`/`overlay.js`, que não existem) · `GUIA_NOVA_CASA.md` com escopo explícito + ponteiro. **(1) CI VERDE de novo — APLICADO (`61d5c8d`, só `tests/`):** a suíte estava vermelha no `main` desde 24/07. `test_captura` usava `kto.bet.br` como exemplo de host "desconhecido" (a s190 registrou a KTO em `_HOSTS_POR_CASA` — o backstop passou a protegê-la, corretamente) e `test_ordem_bet365` ainda exigia o `[Bilhete Bet365]` que a s189 removeu (marcador que o `formatTicketB3` NUNCA emitiu). O código estava certo nos dois; os testes é que envelheceram — e sinal vermelho por ruído deixa de avisar quando quebra de verdade. Fix: `kto.bet.br`/`pinnacle.bet.br` viraram **asserções positivas** (antes soltas), o exemplo de desconhecida virou `kingpanda.bet.br` (casa de print, com comentário fixando a regra) e o split passou a ancorar em `[Código: BR…]`. **193 passed, 4 skipped.** Backup `Backups/ci-verde-2026-07-25/`. **(2) bet365 RELIGADA na conferência de cobertura — APLICADO (`831f97f`):** o gabarito era regex de FORMATO, uma por casa, e a bet365 (`JR8714690761I`) nunca entrou → o guarda da s179 estava desligado na casa de maior lote. Virou **regex genérica de marcador** (`_ID_MARCADOR_RE`), espelho do split do chunker: a fronteira que fatia é a que cobra, e **casa nova entra sem regex nova**. **Conferido no banco antes de ligar** (falso faltante geraria repescagem + linha duplicada): 1.507 códigos bet365, 100% na forma `AA9999999999A`, **zero** grupo de mesmo-conteúdo com códigos parecidos; os 130 pares a ≤2 chars são **sequenciais** (`QR1560103381I`/`382I`/`383I`) — por isso mesmo o código bet365 **não** entra no `corrigir_codigos_tsv` (snap trocaria código válido pelo do vizinho; `_ID_MINLEN=16` já barra). `[Código: ]` vazio não vira gabarito. +4 testes (cadeia ponta a ponta com o texto real do `formatTicketB3`). **(3) Pinnacle no chunking + pré-dedup — APLICADO (`7cf11ae`):** emitia `[Código:]` desde a s170 mas não estava em nenhuma das duas tuplas → **456 bilhetes resolvidos re-enviados à IA a cada recaptura** (medido no banco). As duas tuplas literais (repetidas a ~1100 linhas de distância) viraram **lista única** `_CASAS_MARCADOR_CODIGO`; o próprio `audit_sharpenup` acusou quando elas viraram lista nomeada e passou a exigir que os dois pontos de uso consultem a lista. Aberto segue sendo reprocessado (`get_codigos_resolvidos` só descarta `resolvida`). **(4) Documentação canônica — APLICADO (`101e70f`):** `captura.py` e `index.html` diziam que KTO é "texto sem injetor" (falso desde a s192) e o `CASA_BET365.md` — que vai inteiro para o prompt — ensinava o marcador `[Bilhete Bet365]` (nunca emitido, removido na s189) e um fallback de DOM removido na s182. **Placar: 5 achados de ação → 5 corrigidos (199 passed, audit_sharpenup 14/14 OK). `extensor/*.js` e `manifest.json` INTACTOS — nada exige recarregar a extensão.** **Achados estruturais ainda abertos (dependem de decisão):** **(2) bet365 fora da conferência de cobertura** — `repository.py` não reconhece `JR8714690761I`, então o guarda da s179 (39 de 61 bilhetes sumiram sem erro) está **desligado na casa de maior lote**: um chunk de ~28 bilhetes pode evaporar e a tela diz "✓ N novo(s)". **(3) Pinnacle fora do pré-dedup** (`main.py:1741`) — paga IA de novo por todo bilhete já resolvido, a cada recaptura. **(4) comentários canônicos mentindo** (`captura.py:45-48` e `index.html:3162-3164` dizem que KTO é "texto SEM injetor" — falso desde a s192) e `CASA_BET365.md §2` ensinando à IA um marcador (`[Bilhete Bet365]`) e um fallback de DOM que não existem mais. **Próximo no backlog:** origem nos listeners (integridade) → harness das 6 casas restantes → só então o refactor `roboPassivoGenerico`/`su_hook.js` (que derruba casa nova de ~300 para ~40 linhas, mas mexe em 7 casas que funcionam). Backup `Backups/sharpenup-auditoria-padrao-2026-07-25/`. Ver [[extensor_captura]] · [[extracao_perda_silenciosa_chunk]] · [[feedback-isolar-mudanca-nao-quebrar]]. **Anterior: s193 abaixo.**)_

_Anterior: 2026-07-25 (sessão 193 — **Betfair destravada: a casa trocou `"18-jul-26"` por `"18-jul.-26"` (mês abreviado COM PONTO) e isso zerou 5 dias de bilhetes.** **Sintoma:** o Feca extraiu várias vezes e "não vinha nada". **Prova, não dedução:** o Postgres mostrou última gravação de Betfair em **20/07 02:27 UTC** (códigos `O/…0001770-1777`) e **zero** linha criada OU atualizada desde então, enquanto 534 bilhetes de 9 outras casas entraram no mesmo dia → nada de sessão/ponte/backend. `uso_tokens` mostrou **4 extrações de Betfair no dia, US$ 0,83, ~5,4k tokens de output cada** → a IA rodou e produziu. Os logs do Railway (`victorious-generosity`/`extrator`) não trouxeram `cobertura:` nem `id-fix` → o TSV voltou **completo e com os códigos certos**. Logo, a perda era depois da IA. **Causa raiz, achada no JSON cru da API (o Feca colou o `POST /activity/sportsbook` — ele cobrou "não é mais fácil investigar a API?" e estava certo, foi o caminho curto):** `"placedDate": "17-jul.-26 13:45:15"`. O `_dbrBF` (`content.js:751`) casava `([a-zç]{3})` e esperava `-`, encontrava `.` → **data vazia em 100% dos bilhetes**. **Fix:** ponto opcional (`\.?`) — estritamente permissivo, o formato antigo segue casando (testado: `18-jul.-26`→`18/07/2026` · `12-jul-26`→`12/07/2026` · `15-set.-26`→`15/09/2026` · lixo→vazio). **Por que UM ponto derrubou tudo (o amplificador, ainda ABERTO):** `Data` é a 1ª coluna do TSV; vazia, ela vira um TAB inicial, e o `parse_tsv` (`repository.py:287`) faz `line.strip()` que **engole esse TAB** → todas as colunas deslocam uma casa (`odd` recebe `'W'`) → `validar_linhas` rejeita 100% → `/salvar` devolve "0 novos · N alertas" e **não grava nada, sem erro**. Reproduzido com o texto real do Feca (6/6 linhas rejeitadas, `odd inválido — valor 'W'`). É bug **GERAL**, não da Betfair: vale para qualquer casa que legitimamente venha sem data. **O fix do `_dbrBF` subiu carona no commit `9992829` (KTO), sem commit próprio** — trabalho paralelo do KTO estava na mesma árvore. **CORRIGIDO na mesma sessão (commit próprio, backend-only):** (1) `parse_tsv` — `.strip()` → `rstrip("\r\n")` + strip por CAMPO. Testado nos 3 casos: linha sem data entra íntegra (`data=''`, código no lugar certo), linha normal inalterada, aposta aberta sem código inalterada → 3 válidas, 0 rejeitadas (antes a 1ª era rejeitada). (2) `_betfair_data` (`main.py:440`) — ponto opcional, gêmeo do `_dbrBF`; comentário cruzado nos dois para não dessincronizarem. Suíte: **191 passed, 4 skipped, 2 failed** — as 2 falhas são **PRÉ-EXISTENTES no HEAD** (provado rodando com `git stash`): `test_ordem_bet365::test_build_chunks_bet365_split_por_marcador` (cobra o marcador `[Bilhete Bet365]` que a s189 removeu de propósito — teste ficou stale) e `test_captura::test_casa_de_host_desconhecido_ou_vazio_retorna_none`. **Elas deixam o CI vermelho e precisam de um passo próprio.** **PENDENTE:** `formatTicketBF` ecoa `potentialReturn` cru, que agora chega en-US (`"1,380.00"`) — usar `_brl()` do número (exige reload da extensão, fica p/ o próximo). **NÃO validado ao vivo** — o Feca recarrega a extensão (0.6.23) + Ctrl+Shift+R na aba da Betfair, recaptura e processa (esperado: datas preenchidas e os ~5 dias entrando). Ver [[betfair-data-mes-com-ponto]] · [[betfair_captura_json]]. **Anterior: s192 abaixo.**)_

_Anterior: 2026-07-25 (sessão 192 — **KTO vira casa de API (Kambi) — aposenta o robô de texto, que estava perdendo ~90% dos bilhetes.** **Causa PROVADA (contagem, não dedução):** o Feca capturou e vieram 14 "bilhetes". Os 14 **não eram bilhetes** — eram os 14 pedaços da PÁGINA: o `coletar()` do `roboScroll` parte o `document.body.innerText` por **linha em branco**, e a lista da KTO **não tem linha em branco entre cupons** → 1 bloco gigante com o menu + os ~140 bilhetes + o rodapé, mais 13 cacos de cabeçalho/rodapé. A IA recebia esse blob como um chunk só, extraía os primeiros e perdia o resto em silêncio ([[extracao_perda_silenciosa_chunk]]). **O Feca perguntou "não é mais fácil vir por trás, F12, como nas outras casas?" — e era.** A KTO roda em **Kambi**: `GET …/player/api/v2019/ktobr/coupon/history.json?…&range_size&range_start&status`, paginação por offset com `range.more`. **Construído:** `extensor/kto_inject.js` (novo — hook fetch/XHR, normaliza, aprende a requisição real e **repagina até `more:false`**, avançando pelo `range.size` que VOLTA; replay cobre as URLs que a página disparou + variantes de `status`, então **ninguém clica "Mostrar mais"**) · `content.js` (ouvinte `ktoById`, ramo `casa==="kto"` → `roboKTOPassive`, `formatTicketKTO` com marcador `[Código: <couponRef>]`, autodiagnóstico) · `popup.js` (dispatch do injetor + `CASA_HOSTS`) · `manifest` **0.6.22→0.6.23** · `main.py` (KTO entra no chunking e no pré-dedup por `[Código:]`) · `CASA_KTO.md` §2.5 (campos da API) + §5 (de-para `betStatus`). **Armadilhas confirmadas no dado real:** `betOdds` vem **0 em toda perdida** (usar `playedOdds`); stake/odd/line em **milésimos**; ODDÃO+ tem 2 naturezas (`ODDS_BOOST` sobe a odd · `PROFIT_BOOST` sobe o lucro X%) e **`payout ÷ stake` resolve as duas** (= regra global W). **Conciliação da odd (achado do harness):** retorno é arredondado ao centavo e odd declarada é truncada em 3 casas — regra: a declarada vence **se explicar o retorno até o centavo**, senão o dinheiro manda; nunca truncar. **VALIDADO OFFLINE contra o JSON real** (harness carrega os arquivos do repo, não uma cópia): **6/6 cupons** — aberta múltipla 2,0435 · perdida 6 · W 2,15 · boost 15% 2,3226 · boost odd 2 · bet builder 2,2, datas UTC→BRT batendo com o card. `node --check` verde nos 3 JS + manifest, `py_compile` OK. Backup `Backups/kto-inject-api-2026-07-25/`. **NÃO validado ao vivo** — o Feca recarrega a extensão (0.6.23) + **Ctrl+Shift+R na aba da KTO** (recarregar extensão não re-injeta em aba aberta), conecta e clica "Copiar bilhetes". **Ainda sem amostra:** VOID/Nula, Recusado, cashout encerrado, `systemBets`, aposta grátis, stake dividida — o bloco leva o `Status (API)` cru e a leitura financeira, e enum desconhecido **nunca** vira W/L por chute. Ver [[extracao_perda_silenciosa_chunk]] · [[feedback-isolar-mudanca-nao-quebrar]]. **Anterior: s191 abaixo.**)_

_Anterior: 2026-07-25 (sessão 191 — **KTO: o botão "Conectar" acende — o gate da FRENTE que faltava na s190.** **Causa (provada por leitura de código, não deduzida):** a s190 pôs a KTO em modo texto no backend e eu pedi ao Feca que gerasse um código KTO no dashboard — **impossível**: `CASAS_CONECTAVEIS` (`app/static/index.html:3162`) não listava KTO, então `_casaConectavel()` → `aplicarModoCasa()` marcava o `#btn-conectar-ext` com `disabled` + `.is-off` e o title "O SharpenUp ainda não cobre esta casa — envie os prints manualmente". Sem código gerado, **nada** da s190 chegava a rodar. A s190 não estava errada, estava **incompleta**. **Fix:** `'KTO'` no Set + comentário explicando que é casa de texto SEM injetor. **Sem bump de `?v=`** (o Set é código inline do próprio `index.html`, não `.js` externo). **Resto da cadeia conferido linha a linha e de pé:** `modo_da_casa("KTO")="texto"` (`captura.py:49`) · `/captura/conectar` sem whitelist de casa (`main.py:1508`) · `popup.js:169-176` → `inj=null` p/ KTO (correto: sem interceptor de API, só `START_ROBOT`) · `popup.js:303-305` → cai em `isBetSup` (janela de dias + stopId + "Copiar bilhetes") · `content.js:570` → ramo `else` → `roboScroll` genérico · `content.js:496` `re2` casa "24 de jul. de 2026" · `CASA_HOSTS` sem KTO no popup → `hostBate` devolve `true`, **não bloqueia** (só perde a mensagem de erro amigável). Backup `Backups/kto-botao-conectar-2026-07-25/` (só `index.html`). **NÃO validado ao vivo** — o Feca seleciona KTO na Extração (esperado: botão clicável), gera o código, conecta, abre `kto.bet.br/app/.../historico-de-apostas` e clica "Copiar bilhetes". **Adiado p/ pacote separado (exige bump+reload da extensão, não misturar com o código da bet365 s182-189):** `CASA_HOSTS["KTO"]` no popup + favicon `kto.com`→`kto.bet.br` nos 3 mapas. Ver [[feedback-isolar-mudanca-nao-quebrar]]. **Anterior: s190 abaixo.**)_

> **Histórico completo das sessões 190 → 14** → [`docs/HISTORICO.md`](docs/HISTORICO.md)

---

## 1. O que estamos construindo

A base de conhecimento (masters) do scanner de bets. Camada **global** (regra única, muda devagar) + camada **por casa** (traduz cada casa para a língua global). A saída final é **TSV**.

---

## 2. Invariantes (não se quebram)

1. O app **lê** os masters, **nunca escreve** neles. Mudança de regra = diff revisado por humano + commit. Git é a porta de aprovação.
2. O arquivo de casa **traduz** a casa para a língua global; **não redefine** regra global.
3. **Cálculo é global, localização é da casa.** Ex.: "W → Retorno÷Stake" é global; "o retorno está no campo PRÊMIO" é da Superbet.
4. Nenhuma regra nova é aplicada sozinha. Propor como diff, esperar aprovação.

---

## 3. Estrutura-alvo do repo

```
/global/                 (autoridade única — 6 masters)
    MASTER_PIPELINE_2026.md
    MASTER_ESPORTES_2026.md
    MASTER_APOSTAS_2026.md
    MASTER_DESCRICAO_2026.md
    MASTER_RESULTADO_2026.md
    MASTER_OUTPUT_2026.md
/casas/                  (1 arquivo por casa — traduz, nunca redefine)
    CASA_MODELO.md         (gabarito — 15 seções)
    CASA_BET365.md
    CASA_BETANO.md
    CASA_BETESPORTE.md
    CASA_BETFAIR.md
    CASA_BETNACIONAL.md
    CASA_BOLSADEAPOSTA.md
    CASA_JOGODEOURO.md
    CASA_KINGPANDA.md
    CASA_KTO.md
    CASA_LOTTU.md
    CASA_PINNACLE.md
    CASA_POLYMARKET.md     (por API, não IA)
    CASA_SUPERBET.md
    CASA_VITORIABET.md
/golden_set/
    bilhetes/              (print + TSV esperado)
/docs/                   (referências, ADRs, planos, HISTORICO.md)
STATUS.md                  (este arquivo)
```

Os 6 MASTER_*.md vivem em `/global/`; as 15 casas em `/casas/` (Polymarket por API, as demais por IA/texto).

---

## 4. Estado atual

- **Produto no ar** em `sharpen.bet` (dashboard + extração); deploy automático via Railway.
- **Multi-tenant:** vários donos (Feca, Fatuch, Diogo, Jonathan, Lava…) + operadores; dados isolados por `dono` no Postgres (regras de tenancy/dedup no `CLAUDE.md`).
- **Base do Feca:** migração planilha → Postgres **completa e reconciliada**.
- **Casas:** 15 arquivos em `casas/` (extração por IA/texto) + **Polymarket** por API.
- **Fatuch:** dashboard lê a planilha viva do LavaFatuch via Apps Script (leitura por **cabeçalho**, não por posição); coluna `Espelho` = fornecedor. Sem base no Postgres (tudo vem da planilha).
- **Captura:** extensão **SharpenUp** (moldura+Snap e robô de rolagem) no ar, pareando por código. Casas por **API** (injetor no mundo MAIN, dado exato): Superbet, BETesporte, Betano, Betfair, Pinnacle, Bet365 e **KTO** (Kambi, desde a s192).
- **Modelo de extração:** Sonnet 4.6 (`config.py`).

---

## 5. Pendências (aguardam bilhete real)

- **Bet365:** §6 rótulo visual do boost · §7 rótulo visual do cashout encerrado
- **Betano:** §5 rótulo de void/anulada · §6 boost (existe?)
- **Pinnacle:** §5 rótulo exato de HW/HL no export (precisa de Asian Handicap de quarto liquidado)
- **Bolsa de Aposta:** §5 V/HW/HL · §6 boost · §7 cashout · §8 bônus · apostas Lay
- **Betnacional:** §5 HW/HL · §5 V (rótulo visual de void) · §7 cashout · §8 bônus
- **Jogo de Ouro:** §5 V/HW/HL · §5 rótulo do card na aba Cashout · §7 cashout · §8 bônus
- **KTO:** de-para do `betStatus` da API para VOID/Nula, Recusado, cashout encerrado e meia-liquidação. Também sem amostra: `systemBets` (`Simples (N)`, `Duplas (X), Triplas (Y)`), aposta grátis e stake dividida (duas entradas em `bets[]`). Confirmados hoje: `WON`, `LOST`, `OPEN`.

**Próximo passo:**
- **Testar a KTO ao vivo (s192, nada validado no navegador).** Recarregar a extensão (**0.6.23**), dar **Ctrl+Shift+R na aba da KTO** (recarregar a extensão não re-injeta em aba já aberta), conectar e clicar "Copiar bilhetes". Não precisa clicar "Mostrar mais". Diagnóstico pelo console: logs `[SharpenUp kto_inject]` com `lista aprendida`, `cupons na resposta` e o `range` de cada página.
- Preencher pendências das casas existentes assim que amostras reais chegarem (ver lista acima).
- **Solto (cosmético):** favicon da KTO aponta para `kto.com`; o domínio real é `kto.bet.br`. Corrigir nos 3 mapas (`extensor/popup.js`, `app/static/index.html`, `app/static/dash/assets/js/data.js` + `inicio.html`).
- **Frente worldwide (nova, plano aprovado):** construir a Fase 1 do [`docs/PLANO_EXTRACAO_WORLDWIDE.md`](docs/PLANO_EXTRACAO_WORLDWIDE.md) (confidence da IA + guardrail de enum) quando o Feca quiser. Fase 0 já validada (zero-shot 94,5% de acerto de categoria). Meta: extração universal + cache aprendido → "+adicionar conta" em autosserviço.

Quando chegar um bilhete novo: abrir o arquivo da casa correspondente, preencher a pendência, rodar o checklist do `CLAUDE.md` se envolver categoria nova.

---

## 6. Rodar / produção

**App em produção:** `https://sharpen.bet/` (www.sharpen.bet → Railway)

Para rodar localmente:
```
cd app
pip install -r requirements.txt
# .env na raiz do Planilhador com ANTHROPIC_API_KEY e DATABASE_URL
uvicorn main:app --reload
# Abrir http://localhost:8000
```

---

## 7. Workflow

- **Backup antes de editar** — sempre em `Planilhador/Backups/<nome-descritivo>/`. Nunca usar `FDC Capital/Backups/` (é compartilhada por outros projetos da empresa).
- Arquivos completos, nunca diffs parciais.
- Uma mudança por etapa aprovada.
- Atualizar este STATUS.md ao fim de cada etapa.
- Projeto tem git + GitHub (`flrcarvalho/sharpen`, renomeado de `extrator` na sessão 129). Deploy automático via Railway conectado ao GitHub — push dispara deploy.
