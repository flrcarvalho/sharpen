# STATUS — Masters & Casas (FDC Capital / Planilhador)

Documento de rehydration de sessão. Quem abrir o Claude Code neste repo lê isto primeiro.

> ⚠️ **STATUS ≠ fonte de regras.** Este arquivo é um **changelog/rehydration** (o que mudou e por quê). As regras vinculantes vivem nos **`global/MASTER_*`** (domínio de apostas), em **`pack/tokens/tokens.css`** + **`pack/CLAUDE.md`** (marca/design) e nos **`CLAUDE.md`** (operacional). Não decida uma regra consultando o STATUS — siga o ponteiro para o canônico.

Repo local: `C:\Users\Fernando\Downloads\FDC Capital\Planilhador`

_Atualizado: 2026-08-06 (sessão 249 — **A Jonbet capturou ao vivo na 1ª tentativa e mesmo assim a grade voltou VAZIA: o commit da s248 quebrou, sozinho, a grafia da casa que já existia no banco.** O Feca capturou, o RAIO-X deu 6 itens / 0 duplicadas / 100% e a grade estampou `0 apostas`, com o guard vermelho da s195 — `✗ 6 salvo(s) em «Feca [Eu]» — não aparecem na conta ativa «Feca [Eu]»`. **O dado nunca esteve em risco:** os **13 bilhetes** estavam no banco, íntegros (badminton, códigos de 19 dígitos, stakes com decimal certo, 3 W / 7 L / 3 abertas). Divergia só a **grafia da casa** — a conta em `parceiros` dizia `JonBet`, os bilhetes gravaram `Jonbet`, e o filtro da grade é `casa = $1` **exato** (`repository.py:1169`). **A causa é uma regressão do próprio commit da s248, não um defeito da captura.** O `/salvar` roda a casa da conta por um round-trip (`main.py:2354`): `_casa_display(_display_to_key(casa))`. Enquanto `JONBET` **não** estava no `_CASA_DISPLAY`, `_display_to_key` caía no ramo verbatim e devolvia `JonBet` intacto — **o round-trip era identidade e a conta funcionava**. Ao registrar a casa no mapa, a chave passou a casar e a voltar `Jonbet`: a partir do deploy, todo bilhete nasceu numa casa que **conta nenhuma enxerga**. Confirmado por `git show HEAD~1:app/main.py` (**0** ocorrências de `JONBET`) e pelo horário — as 2 contas nasceram à mão às 16:34 UTC de 06/08 e em 03/08, **antes** do deploy (~19:05 UTC). **Regra que fica: registrar uma casa no `_CASA_DISPLAY` é mudança RETROATIVA — ela reinterpreta toda conta que já existia naquela grafia.** Casa nova só é inofensiva enquanto ninguém cadastrou conta antes do mapa. **Medida a extensão do estrago antes de mexer:** das **57** grafias distintas de casa em `parceiros`, **exatamente 1** tinha round-trip quebrado. Não era iceberg. **Efeitos colaterais silenciosos da mesma divergência**, todos coerentes com o print: favicon ausente (`DOMINIOS[casa]` sem match, avatar vazio) e **a guarda de domínio do popup DESLIGADA** — `CASA_HOSTS["JonBet"]` é `undefined` e `hostBate` devolve `true` sem checar nada. A captura só funcionou porque o `jb_inject.js` está no `manifest` como content script; o caminho de `executeScript` do popup (`casa === "Jonbet"`) **não** disparou. **Grafia escolhida por evidência, não por gosto:** a marca escreve `Jonbet` (conferido em `jonbet.bet.br`) e é o que o código e o `CASA_JONBET §1` já diziam — quem divergia eram as 2 contas digitadas à mão. **Correção barata porque `bilhetes` já estava do lado certo:** `JonBet` vivia em `parceiros` (2), `casas_meta` (2), `correcoes` (3) e `uso_tokens` (2) — e em **zero** bilhetes. Logo **nenhuma assinatura foi recalculada** e não há risco de duplicar histórico na próxima captura. Aplicado com `scripts/unificar_casas.py` (a ferramenta que já existia, com transação, checagem de colisão, backup e idempotência) — **9 linhas, 0 bilhetes, 0 colisões**. **O relatório pegou um resíduo que não era da sessão e que teria entrado de carona:** **42 bilhetes do `LavaPessoal`** (`Faz1bet` → `Faz1Bet`), porque a base dele foi importada na **s222**, depois da unificação da **s199**, e ressuscitou a grafia velha. **O `MAPA` é cumulativo: corrigir uma casa arrastaria a base de outro dono na mesma transação, recalculando 42 assinaturas.** Daí o `--somente <grafia>` novo, que recorta a aplicação a uma entrada e mantém o relatório mostrando o mapa inteiro, para o resíduo não sumir de vista. **Verificado pelo MESMO filtro da grade, com a casa lida da conta e não digitada no teste:** Feca **0 → 13 bilhetes**, Diogo agora em `Jonbet` (0 bilhetes, ainda não capturou), uma só grafia em `bilhetes`, round-trip quebrado **0 de 57**. **Confirmação de brinde da decisão de desenho da s248:** as datas saíram 04, 05, 06 e **07/08**, e as 3 abertas são exatamente as de 07/08 — eventos futuros. Fosse a data de **colocação**, as três estariam carimbadas em 05 ou 06/08. A coluna Data do evento provou-se ao vivo. Backup `Backups/jonbet-grafia-casa/`. **Anterior: s248 abaixo.**)_

_Anterior: 2026-08-06 (sessão 248 — **"Com o F12 eu não achei o que precisamos": a Jonbet entra no SharpenUp por API + replay, e a requisição estava escondida por dois motivos que valem para qualquer casa nova.** A Jonbet **não tem sportsbook próprio** — ela embute o **BetBy** (`sptpub.com`), carregado por `jonbet.sptpub.com/bt-renderer.min.js` **na própria página** (⚠️ **não é iframe**: o `content.js` alcança tudo, nada do inferno do frame de membros da bet365). Daí os dois motivos: o tráfego útil sai em `api-31-sp-<hash>.sptpub.com` e **não** em `jonbet.bet.br`, e um long-poll dispara `api/v4/live|prematch/...` **a cada ~2 s**, soterrando a chamada da lista sob centenas de linhas de ruído. **Regra que fica: em casa que não revela o endpoint, filtre o Network pelo PATH (`my_bets`, `history`, `coupon`), nunca pelo domínio da casa.** O endpoint foi localizado **sem login e sem tocar na conta**, por engenharia reversa do bundle do motor (`start31.sptpub.com/static/js/App.*.chunk.js`): `GET {apiUrl}/api/v1/my_bets/list?lang=pt-BR&skip=&limit=15&status=&currency=BRL` com `Authorization: Bearer`, devolvendo `{count, results[]}`; do mesmo bundle saíram o **enum de status em string** (`open · won · half-won · half-lost · lost · canceled · refund · cashed out` — com espaço — `· void · useless · rejected · vip-stake-requested`) e a confirmação de que **`status` vazio = todas as abas**. **Quatro armadilhas, todas MEDIDAS contra o card, não deduzidas:** (1) **`total_k` (a odd) vem `"0"` em 6 de 6 PERDIDAS**, com `k` guardando a odd que o card mostra — é o `betOdds` da KTO em outra roupa, e ler o campo cru gravaria **odd zero em 100% das perdas**; a regra é a do próprio app da casa (`parseFloat(total_k)===0 ? k : total_k`), e `result_k` acompanha o zero, então **não** serve de fallback; (2) **`timestamp` é epoch em SEGUNDOS** (float), não ms; (3) **stake é `sum`**, não `stake`, string com **ponto** decimal (`"333.16"`) — nunca o parser de dinheiro BR; (4) **`cashout_amount` vem preenchido na ABERTA** (é a oferta de venda antecipada do botão), então o bloco só emite `Cashout executado:` em bilhete resolvido — a mesma armadilha do `totalWin` da VaideBet, que virava vitória fantasma. **O achado que mudou o desenho no meio da implementação: a coluna Data.** O bloco tinha nascido emitindo só `Data (colocação)`, como a KTO. A medição mostrou que **7 dos 10 bilhetes mudam de dia** entre colocação e evento (colocada `05/08 18:00` → jogo `06/08 01:05`): é casa de **badminton asiático**, jogo de madrugada apostado na tarde anterior. O `MASTER_OUTPUT §4` manda usar a **perna mais recente** e é o que o card estampa (`Hoje, 01:05` · `Ontem, 01:35` · `Anteontem, 22:40`), então o formatador passou a emitir **as duas datas**, com `Data (evento mais recente):` primeiro — o mesmo que VaideBet e bet365 fazem. Emitir só a colocação teria posto **70% da base no dia errado**, exatamente o defeito que a VaideBet levou a produção na s210. **A guarda do token, e por que ela é uma trava de teste e não um comentário:** a página dispara a lista **antes de o token chegar** e toma **401**, com um corpo que **tem uma chave `status`** (`{"description":"Unauthorized","status":401,...}`) que não é status de bilhete. Aprender essa requisição para o replay faria o robô repaginar deslogado e reportar `hook:true` + `respostas>0` + **0 bilhetes** — o mesmo sintoma de "formato mudou". `capturarReq` só guarda requisição **com `Authorization`** e `forward` só processa corpo com `results` array. **A 1ª versão do caso de harness não exercitava essa guarda** (a requisição inicial já ia com token, então o corpo do 401 nunca era servido): foi corrigida para reproduzir a sequência real — 1ª sem token → 401, 2ª autenticada → 200 — e o caso agora **conta** as chamadas sem Bearer, falhando se forem 0 (guarda não exercitada) ou mais de 1 (replay repaginando deslogado). **Paginação PROVADA ao vivo antes de escrever o loop**, forçando `limit=3` sobre os 10 bilhetes da conta: `skip` 0/3/6/9 → 3,3,3,1 (soma exata de `count`, sem ID repetido nem pulado), `count` constante entre páginas, ordem **decrescente por data**, e **`skip=12` devolve 200 com lista VAZIA** — nunca erro, nunca repetição da última página. Daí a parada dupla (`skip >= count` **ou** lista vazia) e o avanço pelo tamanho que **voltou**. O servidor do harness **ignora de propósito o `limit` pedido** e devolve no máximo 3: um inject que avance pelo que pediu pula bilhete, e isso vira vermelho em vez de buraco no histórico. **Entregue:** `extensor/jb_inject.js` (espelho do `kto_inject`), 4 peças no `content.js` (ouvinte com resolvida-vence-aberta, `formatTicketJB`, `roboJBPassive`, ramo no `iniciarRobo` + autodiagnóstico), os **12 pontos de registro** e `casas/CASA_JONBET.md`. `sandbox.mjs` ganhou dois parâmetros **opcionais e aditivos** (`optsInicial`, e `urlsExtra` aceitando `{url, opts}`) — sem eles o harness não teria como reproduzir a sequência 401→200; as 8 casas existentes seguiram verdes na mesma execução. **Detalhe de dedup que ficou documentado porque é sorte, não descuido:** os ids da Jonbet são **quase idênticos entre si** (`…168314475463` × `…168314475401` diferem em 3 chars) e a casa fica **fora do snap por edit-distance** do `corrigir_codigos_tsv` — nenhuma das três regexes de lá casa 19 dígitos, e é assim que deve continuar, pelo mesmo motivo da bet365 (ids sequenciais). A conferência de cobertura segue ligada pelo marcador genérico. **Gates:** harness **verde, 9 casos, 164 bilhetes** (era 8/154) · `audit_sharpenup` **18 casas, sem FAIL** · `audit_casas` sem FAIL · `node --check` nos 3 JS · manifest **0.6.36 → 0.6.37** (PATCH: casa nova sobre mecanismo existente) · `py_compile` · `pytest` **351 passed, 13 skipped** · `check-tokens` verde. Backup `Backups/jonbet-sharpenup/`. **⚠️ NÃO validada ao vivo** — nenhum lote passou pela extensão ainda (ver §5). **Cobertura que a amostra não tem** (a conta só tem 10 bilhetes, todos simples de 04–05/08): cashout executado, múltipla, bet builder, `half-won`/`half-lost`, `void`/`refund` e qualquer bilhete com imposto (`payout_tax` veio `"0"` e `taxes` ausente em toda a amostra). **BetBy é plataforma, não casa:** qualquer outra casa que carregue `sptpub.com` é **casa espelho** — mesmo endpoint, mesmo inject, mesmo formatador, como a Betfast é da Tivo. **Anterior: s247 abaixo.**)_

_Anterior: 2026-08-05 (sessão 247 — **"O sync da Polymarket vem demorando mais de 3 minutos e muitas vezes nem funciona": o gargalo não era a Polymarket, era o Banco Central. O câmbio saía de 111 chamadas sequenciais por data; agora sai de UMA chamada de faixa.** Medido antes de mexer, contra a carteira real (456 resolvidas + 6 ativas): a Polymarket responde em ~3s (4 chamadas ao Worker) e o **PTAX/BCB consumia 113 chamadas**. A conta: **76 datas distintas de bilhete**, mais o recuo de fim de semana/feriado (`_cotacao_para` tentava até 10 dias, um pedido de rede por dia tentado), mais 6 do `_ptax_hoje`. **Custo antigo ponta a ponta, com o BCB saudável: 25,4s** (179ms por chamada dentro do cliente persistente). **Número corrigido no meio da sessão:** eu havia estimado ~190s a partir de 1,7s por chamada — essa medição usava conexão nova a cada vez (handshake TLS por pedido) e não representava o app; o diagnóstico não mudou, o número sim. **O que explica os 3 minutos e o "nem funciona" é o MULTIPLICADOR, não a latência unitária.** O BCB oscila: na 1ª execução da sessão o `_ptax_hoje` sozinho gastou **40,4s e voltou `None`** (endpoint por dia devolvendo 502 em série; taxa medida à parte: 1 falha a cada 6 chamadas). Com 502/timeout, cada chamada carrega 3 tentativas + 1s + 2s de backoff (`_get_retry`) — 113 chamadas viram minutos. E o desenho tinha um segundo defeito, pior: **`_ptax` devolve `None` tanto para "não houve boletim nesse dia" quanto para "o BCB falhou"**, indistinguíveis. Um soluço de rede consumia os 10 recuos de uma data e derrubava o sync inteiro com `CambioIndisponivel` → 503. Três minutos de trabalho perdidos por um timeout. **Fix em `app/polymarket.py`:** entra `BCB_PTAX_PERIODO` (`CotacaoDolarPeriodo`), que devolve a faixa toda de uma vez — **3 anos de PTAX = 902 registros em ~1,3s**, sem teto de paginação, e data final no futuro é aceita (o BCB corta em hoje). `_carregar_periodo` indexa por dia num **mapa de módulo** (`_PTAX_MAPA`), que vive **entre requisições**: cotação de dia passado é imutável, então o 2º sync não gasta rede nenhuma. `_cotacao_do_mapa` resolve em memória o mesmo recuo de 10 dias que antes era rede. `_garantir_cobertura` carrega o que falta e **levanta `CambioIndisponivel` quando o BCB cai** — a falha volta a ser falha, e aborta em segundos em vez de moer o histórico até desistir. `_inicio_hint` deduz da `activity` a compra mais antiga para a 1ª carga já pedir a faixa inteira (uma chamada, não uma janelinha em torno de hoje). `_ptax` (endpoint por dia) sobrevive **só** como rede de segurança do `_ptax_hoje`, no máximo 6 chamadas e para UMA data. **`coletar_dashboard` deixou de ter laço próprio de PTAX** — esse poll roda a cada 60s e não podia pagar rede pelo sub em R$. **Resultado medido, mesma carteira:** **1º sync 4,7s** (1 chamada ao BCB, 0,2s) · **2º sync 2,6s** (zero chamadas ao BCB) · era 25,4s com o BCB de pé e minutos com ele oscilando. **A checagem que importava:** a cotação nova foi comparada com o laço antigo **nas 76 datas de bilhete, uma a uma** — **0 divergências**. Sem isso, um re-sync mexeria em stake já gravado (`origem='sync'` é `_ORIGEM_AUTORITATIVA`: refresca `stake`/`odd`/`data` mesmo em bilhete resolvido). Dois detalhes de desenho garantem essa estabilidade: o mapa usa `setdefault`, ficando com o **primeiro** boletim do dia — o BCB republica um punhado de dias com dois registros (ex.: 23/04/2025, mesmo valor) e o `$top=1` do endpoint antigo também pegava o primeiro; e a janela de recuo continua exatamente 10 dias. **Gates:** `pytest tests/` **351 passed, 13 skipped** · 6 testes novos travando faixa única (N datas = 1 chamada), recuo de 10 dias, escolha do 1º boletim do dia, cobertura reaproveitada entre syncs, `_inicio_hint`, e **BCB fora do ar levantando `CambioIndisponivel` em vez de virar "dia sem boletim"**. **Corrigida de passagem uma dependência de rede escondida na suíte:** `test_coletar_tudo_paridade` não fazia monkeypatch da carga em massa e saía para o BCB de verdade; agora a suíte roda inteira com `httpx` bloqueado (38 passed). Backup `Backups/poly-sync-ptax-faixa/`. **Anterior: s246 abaixo.**)_

_Anterior: 2026-08-04 (sessão 246 — **O calendário do sistema agora é da MARCA: nasce o SharpenCal (`app/static/sharpen-cal.js`), e o popup nativo branco do Chrome sai de cena em todos os campos de data.** Continuação direta da s243: o Feca reportou que "nenhum calendário funcionou em lugar nenhum". **Diagnóstico medido no Chrome dele (150), via automação:** o código da s243 estava carregado e o clique disparava a função, `showPicker()` executava **sem lançar erro** — e mesmo assim nenhum popup aparecia (provado por teclado: o Enter seguinte reativou o botão em vez de escolher dia; popup nativo não sai em screenshot nem recebe tecla injetada, então automação não consegue nem vê-lo nem testá-lo). No teste com os olhos do Feca, o popup nativo **abre** no `type=date` visível do filtro — mas veio o requisito decisivo: **o popup nativo não tem NADA da marca** (branco, sem tokens) e o Chrome não aceita estilo nele; e o Feca pediu calendário também na edição de data **das linhas** (Minha Base inline e lista da Extração). Decisão: parar de depender do popup nativo. **SharpenCal:** popover DOM único por página, 100% tokens (`--surface-2`/`--line`/`--ink*`/`--accent`/`--r-md`/fontes da marca), pt-BR (D S T Q Q S S, "Agosto de 2026"), hoje com borda accent, selecionado accent, dias fora do mês em `--ink-mute` 12px, rodapé Limpar/Hoje em `--accent-2`; aceita e devolve `DD/MM/AAAA` ou ISO (`opts.saida`); fuso local sempre. Dois detalhes de desenho load-bearing: **`mousedown` com `preventDefault` no popover** (não rouba o foco do editor inline — senão o blur salvaria antes do clique no dia chegar) e **Esc em captura fecha só o calendário** (o modal por trás fica). **Ligado em TODOS os pontos de data:** modal da Extração (`#ed-data`, botão + duplo-clique — cobre a edição das linhas da grade, que passa pelo ✎), `#data-ref` da captura (indicador nativo escondido via CSS + botão; Limpar volta a hoje), modal do dashboard (`#ap-ed-data` via `apEdAbrirCalendario`), **edição inline da Minha Base** (duplo-clique na célula de data abre input + SharpenCal juntos; escolher dia preenche e salva via o `finish` existente), filtros de Período (`filters.js`, `abrirCalFiltro` grava ISO + dispara change para o `setDateF` inline) e escada de unidade (`tmData`, `gestao.js::tmAbrirCal`). CSS genérico `.shcal-datewrap`/`.shcal-databtn` no `components.css` substitui o `apedit-data-wrap` da s243; inputs `type=date` embrulhados perdem o `::-webkit-calendar-picker-indicator`. `.date-row input[type=date]` (seletor descendente) sobrevive ao embrulho — conferido antes. CSP conferida antes do commit: `style-src 'unsafe-inline'` permite o `<style>` que o componente injeta. **Gates (`/nova-ui`):** `check-tokens` verde · `node --check` nos 5 JS + `sharpen-cal.js` · 3 blocos inline do `index.html` por `vm.Script` · Escada de Tinta do componente: label de semana 9,5px `--ink-soft`, dia 12px `--ink`, fora-do-mês `--ink-mute` 12px (≥ piso de metadado), `#fff` só sobre `--accent` (precedente `.apedit-btn-primary`). Cache-bust: `sharpen-cal.js?v=1` (novo) · `components.css?v=21` · `filters.js?v=9` · `gestao.js?v=27` · `apostas.js?v=14` · `app.js?v=33`. Backup `Backups/sharpen-cal-calendario-marca/`. **Verificação em produção por screenshot** (agora possível: o calendário é DOM) na sequência da sessão. **Anterior: s245 abaixo.**)_

_Anterior: 2026-08-04 (sessão 245 — **"Apostas de badminton que não conseguiram ser classificadas no esporte correto": a lista auxiliar era o top-50 BWF, mas o bilhete real é de circuito secundário — e a casa escreve o nome do atleta na ordem invertida.** Print do Feca com 2 bilhetes da Superbet (`leogois03 [Richard]`, tipster **Bad Milton**, 03/08) em `Outro`: `Shogo Ogawa [Shogo Ogawa v Zi Jun Wang]` e `Over 69.5 Pontos [Chao Sun v Hyeon Woo Cho]`. **Medido no Postgres ANTES de editar, e o quadro é menor e mais informativo do que a tela sugere:** a base tem **79 bilhetes Badminton** classificados certo e **só 2** em `Outro` hoje — e as **mesmas duas apostas**, capturadas na **Bet365 e na Betano** no mesmo dia, saíram `Badminton`. As 5 linhas `Outro` do tipster CP4 que também são badminton são de **15–17/07**, ou seja **antes do badminton existir nos masters** (s174, 22/07) = legado, não regressão. **O lote real mata a hipótese fácil:** a mesma leva (04/08 19:27 UTC, 4 chunks, mesma casa e mesma conta) trouxe `Geonyeop Cho v Byung Jae Kim` como **Badminton certo** e os dois outros errados → não é falta de contexto da casa, é **decisão marginal** entre Tênis e Badminton. **As duas causas, isoladas por A/B contra a API real** (os 6 masters + `CASA_SUPERBET`, com o backup servindo de braço "antes"): (1) as `Referências auxiliares — Badminton` são o **top-50 BWF de jun/2026**, e o bilhete real é de **Super 100 / China Masters / liga doméstica / juvenil** — Shogo Ogawa é **#71** ([Wikipedia](https://en.wikipedia.org/wiki/Shogo_Ogawa)), Cho Hyeon-woo é **#172** e jogou a Thomas Cup 2026 ([BWF](https://bwfworldtourfinals.bwfbadminton.com/zh-cn/player/86283/cho-hyeon-woo)), Sun Chao é campeão do **Ruichang** e do **Baoji China Masters 2026** ([Wikipedia](https://en.wikipedia.org/wiki/Sun_Chao_(badminton))); (2) **o achado que ninguém tinha visto: a casa inverte a ordem do nome.** `Yu Qi Shi` é o **nº 1 do mundo** (listado como `Shi Yuqi`) e mesmo assim virava **Tênis**, porque a comparação com a lista era literal. Vale para `Zhi Yi Wang`/Wang Zhiyi, `Guang Zu Lu`/Lu Guangzu, `Hong Yang Weng`/Weng Hongyang, `Jia Heng Jason Teh`/Jason Teh Jia Heng, `Ka Long Angus Ng`, `V. Sindhu Pusarla` — todos presentes na base. **Editado só o `MASTER_ESPORTES_2026.md` (4 blocos, §7 e §8):** (a) `### Referências auxiliares — Badminton (circuito secundário)` — tabela de **como a casa escreve × como o ranking escreve**, a regra de comparar por **conjunto de partes do nome** (ignorando ordem/hífen/caixa) e ~40 atletas **observados em bilhete real** (procedência declarada: é base, não ranking); (b) **assinatura de mercado** nos Contextos auxiliares — total ou handicap de **Pontos** num confronto entre **pessoas** é badminton, porque o tênis não tem esse mercado (usa games/sets) e os dardos contam `legs`; faixa **medida na base**: 67,5 · 68,5 · 69,5 · 71,5 · 72,5 · 76,5 · 78,5 · 79,5 · 80,5 → regra escrita como 55–95, com a fronteira contra Basquete/Vôlei (lá o confronto é entre **times**) explícita; mais os torneios do circuito secundário (China Masters Ruichang/Baoji/Wuxi, Super 100, International Challenge/Series, ligas domésticas, mundial juvenil); (c) a **Regra Crítica — Badminton vs Tênis vs Dardos** ganha 3 itens novos (mercado de pontos = sinal positivo sozinho · comparação de nome por conjunto de partes · **conhecimento próprio do modelo É sinal positivo**, o §5 item 4 autorizava e a Regra Crítica não repetia) e o **⛔ `Outro` deixa de ser saída para confronto de raquete** — dois participantes legíveis sempre têm esporte, e o desempate segue sendo **Tênis**; (d) §8 ganha os itens **21–23**. **Prova por medição, e o primeiro teste foi DESCARTADO por não provar nada:** o par exato que falhou em produção, rodado isolado, sai `Badminton` **nos dois braços** — teste que passa antes e depois não é evidência. Redesenhado para atacar as regras novas, 2 rodadas por braço, resultado idêntico nas duas: **ANTES `[Badminton, Tênis, Tênis]` · DEPOIS `[Badminton, Badminton, Badminton]`** (caso 1 = mercado de pontos com nomes propositalmente desconhecidos, já acertava; caso 2 = `Hyeon Woo Cho v Sung Seung Na`, circuito secundário sem sinal de mercado; caso 3 = `Yu Qi Shi v Guang Zu Lu`, nome invertido de dois atletas **que já estavam na lista**). **Gates:** `pytest tests/test_taxonomia.py` **8 passed** e o parse da §7 segue com **21 esportes** — as seções novas são **H3 de propósito**: um `##` ali dentro viraria esporte falso no menu de todo usuário (`app/taxonomia.py` lê os H2 da §7) · `audit_casas` sem FAIL. Backup `Backups/badminton-circuito-secundario/`. **As 2 linhas foram CORRIGIDAS na mesma sessão** (ids 125105/125106 → `Badminton`) com `scripts/corrigir_esporte_bilhete.py` (novo): recebe ids explícitos conferidos por humano, padrão relatório, `--aplicar` grava, e **reusa `atualizar_bilhete`** em vez de UPDATE cru — assim a trilha em `correcoes` sai gravada (2 linhas, `esporte: Outro → Badminton`) e a assinatura é reavaliada (não muda: `esporte` ∉ `_SIG_COLS`, `repository.py:669`). **Levantamento pedido pelo Feca, base dele:** **21** bilhetes de badminton ativos (19 antes da correção) em **2 tipsters** — `Bad Milton` 20 bilhetes / turnover R$ 5.365,00 / P/L **+R$ 547,17** (era 169,67; +377,50 = os dois W que estavam fora do esporte) e `Arrudex` 1 bilhete / +R$ 375,00 (subiu no mesmo segundo que dois do Bad Milton, mesma conta, com tipster posto à mão — `origem_tipster='humano'`, então é escolha e não deslize). Mais **11 arquivados** fora da visão padrão, todos com código bet365 real: 7 do Bad Milton (04/08, `marloncezar01`, −R$ 1.175,41) e 4 de teste do tipster `Feca` (21/07, stake R$ 0,50, todos V). **Varredura por badminton escondido em `Tênis`/`Outro`** (por nome de atleta E por faixa de mercado): **zero** — os 18 candidatos que o filtro pegou são todos legítimos de **times** (eBasket, Rugby, Basquete), a fronteira que a regra nova declara. **Não aplicado, aguardando decisão (ver §5):** (1) o `formatTicket` da Superbet (`extensor/content.js:911`) **não emite a linha `Esporte (casa): … · Liga`** que Betfair (`:1169`), Pinnacle (`:1335`), KTO, Betfast e BETesporte emitem — daria contexto determinístico a **todos** os esportes dessa casa, mas exige confirmar os nomes dos campos no JSON de `/user/{id}/tickets` (F12 → Network) em vez de chutar; (2) categoria, não esporte: 5 bilhetes Betano de 10–11/06 (`Over 78.5 Pontos [dupla v dupla]`) estão em `Player Props` sendo total da partida → `Pontos`, e 9 estão em `Games` onde o `MASTER_APOSTAS §6` manda `Sets`. **Anterior: s244 abaixo.**)_

> **Histórico completo das sessões 244 → 14** → [`docs/HISTORICO.md`](docs/HISTORICO.md)

---

## 1. O que estamos construindo

A base de conhecimento (masters) do scanner de bets. Camada **global** (regra única, muda devagar) + camada **por casa** (traduz cada casa para a língua global). A saída final é **TSV**.

---

## 2. Invariantes (não se quebram)

1. O app **lê** os masters, **nunca escreve** neles. Mudança de regra = diff revisado por humano + commit. Git é a porta de aprovação.
2. O arquivo de casa **traduz** a casa para a língua global; **não redefine** regra global.
3. **Cálculo é global, localização é da casa.** Ex.: "W → Retorno÷Stake" é global; "o retorno está no campo PRÊMIO" é da Superbet.
4. Nenhuma regra nova é aplicada sozinha. Propor como diff, esperar aprovação.

---

## 3. Estrutura-alvo do repo

```
/global/                 (autoridade única — 6 masters)
    MASTER_PIPELINE_2026.md
    MASTER_ESPORTES_2026.md
    MASTER_APOSTAS_2026.md
    MASTER_DESCRICAO_2026.md
    MASTER_RESULTADO_2026.md
    MASTER_OUTPUT_2026.md
/casas/                  (1 arquivo por casa — traduz, nunca redefine)
    CASA_MODELO.md         (gabarito — 15 seções)
    CASA_BET365.md
    CASA_BETANO.md
    CASA_BETESPORTE.md
    CASA_BETFAIR.md
    CASA_BETNACIONAL.md
    CASA_BOLSADEAPOSTA.md
    CASA_JOGODEOURO.md
    CASA_KINGPANDA.md
    CASA_KTO.md
    CASA_LOTTU.md
    CASA_PINNACLE.md
    CASA_POLYMARKET.md     (por API, não IA)
    CASA_SUPERBET.md
    CASA_TIVO.md
    CASA_BETFAST.md        (espelho técnico da Tivo — mesmo motor BetConstruct)
    CASA_VAIDEBET.md
    CASA_VITORIABET.md
/golden_set/
    bilhetes/              (print + TSV esperado)
/docs/                   (referências, ADRs, planos, HISTORICO.md)
STATUS.md                  (este arquivo)
```

Os 6 MASTER_*.md vivem em `/global/`; as 17 casas em `/casas/` (Polymarket por API, as demais por IA/texto).

---

## 4. Estado atual

- **Produto no ar** em `sharpen.bet` (dashboard + extração); deploy automático via Railway.
- **Multi-tenant:** vários donos (Feca, Fatuch, Diogo, Jonathan, Lava, LavaPessoal…) + operadores; dados isolados por `dono` no Postgres (regras de tenancy/dedup no `CLAUDE.md`). Identidade na tabela `usuarios` do Postgres via cache em memória (s233 — Fase 1 do `docs/PLANO_MULTIUSUARIO_2026.md`); os dicts de `app/auth.py` são a SEMENTE. Conta nova = 1 linha em `USUARIOS` (`app/auth.py`) + `SENHA_<USER>_HASH` no Railway (o seed leva ao banco no boot); base nasce vazia sem migration. Suspender no banco (`status`) revoga login E sessão em ≤60s.
- **Base do Feca:** migração planilha → Postgres **completa e reconciliada**.
- **Base do `LavaPessoal` (s222):** 2.877 apostas importadas do `.xlsx` pessoal do Lava (23/02 → 30/07/2026), `origem='import'`, conta `Padrão` em cada uma das 19 casas (ele não anota fornecedor). Script próprio e idempotente: `scripts/import_lavapessoal_xlsx.py` (re-rodar limpa só `origem='import'` daquele dono; captura da extensão sobrevive). **Não confundir com o dono `Lava`** — são bases distintas que só compartilham o apelido. **O P/L do dashboard não bate com a planilha de origem por desenho** (ela contabiliza em unidade; ver s222 no topo).
- **Base do `SoChutes` (s224):** 23.199 apostas all-time do tipster Só Chutes (17/09/2024 → 27/07/2026) importadas do `.xlsx`, `origem='import'`, conta `Padrão` (Bet365/Superbet/Betano; casa não informada entrou como Bet365 — decisão do Feca). **Stake em UNIDADES** (1u = 1; o P/L do dashboard é o P/L em unidades: +1.381,29u). Script idempotente: `scripts/import_sochutes_xlsx.py`. O planilhamento novo é do **bot Sharpen** (repo próprio, ver s223).
- **Casas:** 17 arquivos em `casas/` (extração por IA/texto) + **Polymarket** por API.
- **Fatuch:** dashboard lê a planilha viva do LavaFatuch via Apps Script (leitura por **cabeçalho**, não por posição); coluna `Espelho` = fornecedor. Sem base no Postgres (tudo vem da planilha).
- **Captura:** extensão **SharpenUp** (moldura+Snap e robô de rolagem) no ar, pareando por código. **10 casas por API** (injetor no mundo MAIN, dado exato): Superbet, BETesporte, Betano, Betfair, Pinnacle, Bet365, KTO (Kambi, s192), Tivo (s196), VaideBet (Altenar, s210) e **Betfast** (s211 — **espelho da Tivo**: mesmo motor BetConstruct, mesmo `tv_inject.js`, sem código duplicado).
- **Apostas em aberto (s215):** o feed (`dashboard_rows`) carrega a aposta não liquidada marcada `resultado='ABERTA'`, `lucro=0`. Ela aparece no topo da **Minha Base** (ex-"Apostas") e tem tela própria em **Minhas Apostas › Em Aberto** (`charts/abertas.js`): KPIs de exposição, horizonte por faixa de dia, calendário por data do evento, barras por casa e por tipster, lista completa. **Nenhuma métrica a soma** — `aplicarFeed` separa `DADOS` (encerradas) de `DADOS_ABERTAS`, e Início/Extração cortam por `resultado==='ABERTA'`.
- **Modelo de extração:** Sonnet 4.6 (`config.py`).

---

## 5. Pendências (aguardam bilhete real)

- **Bet365:** §6 rótulo visual do boost · §7 rótulo visual do cashout encerrado
- **Betfair:** cashout **parcial** (`isPartialCashOut`) sem amostra — o total já está travado no harness (2 casos) · HW/HL sem amostra · Each Way com `0 < Retorno < Stake` (o §5 não cobre essa faixa; hoje sai "a conferir", sem chute)
- **Betano:** §5 rótulo de void/anulada · §6 boost (existe?)
- **Pinnacle:** §5 rótulo exato de HW/HL no export (precisa de Asian Handicap de quarto liquidado)
- **Bolsa de Aposta:** §5 V/HW/HL · §6 boost · §7 cashout · §8 bônus · apostas Lay
- **Betnacional:** §5 HW/HL · §5 V (rótulo visual de void) · §7 cashout · §8 bônus
- **Jogo de Ouro:** §5 V/HW/HL · §5 rótulo do card na aba Cashout · §7 cashout · §8 bônus
- **KTO:** de-para do `betStatus` da API para VOID/Nula, Recusado, cashout encerrado e meia-liquidação. Também sem amostra: `systemBets` (`Simples (N)`, `Duplas (X), Triplas (Y)`), aposta grátis e stake dividida (duas entradas em `bets[]`). Confirmados hoje: `WON`, `LOST`, `OPEN`.

- **Betfast / Tivo (s211):** cashout · bônus · aposta de sistema · outright · **aposta ABERTA** (as 50 da amostra são liquidadas) · `§9` de duas categorias (`Total de defesas do goleiro` · `Handicap de mapas`/`Map Advantage`)
- **Jonbet:** ~~captura NÃO validada ao vivo~~ **VALIDADA na s249** — a extensão capturou na 1ª tentativa e gravou **13 bilhetes** (3 W · 7 L · 3 abertas), com código, stake e odd conferidos no banco. **A coluna Data ficou provada ao vivo:** saiu 04/08 (3) · 05/08 (4) · 06/08 (3) · **07/08 (3)**, e as 3 abertas são justamente as de 07/08 — eventos futuros, que a data de **colocação** teria carimbado em 05 ou 06/08. **Falta só a conferência visual do Feca contra o card** (contagem e datas lado a lado). Segue sem amostra: **cashout executado** · múltipla · bet builder · sistema · `half-won`/`half-lost` · `void`/`refund`/`rejected` · **boost** (`boost:false` em tudo) · **imposto** (`payout_tax:"0"` e `taxes` ausente — quando aparecer, decidir de uma vez se o `W` usa retorno bruto ou líquido, ver `CASA_JONBET` Feedback #2). `§9` só tem os 3 mercados de badminton confirmados.

- **`LavaPessoal`: 42 bilhetes em `Faz1bet` esperando a unificação da s199** (achado de passagem na s249, ao rodar o relatório do `unificar_casas.py`). A base dele foi importada na **s222**, depois da unificação, e trouxe de volta a grafia que a s199 tinha aposentado — `Faz1bet` → `Faz1Bet`. **Não aplicado de propósito:** é base de outro dono e mexe em `casa` de bilhete, então **recalcula 42 assinaturas** (a decisão da s234 vale: não tocar sem o dono pedir). Quando for: `python scripts/unificar_casas.py --somente Faz1bet` para o relatório, depois `--aplicar`. **A lição maior é do `MAPA`, não do LavaPessoal:** ele é cumulativo e **todo import futuro pode ressuscitar qualquer grafia já aposentada** — rodar o relatório sem filtro de tempos em tempos é o que revela isso.

- **Blindar o round-trip de casa no `/salvar` (proposto na s249, NÃO feito).** Quando a conta é resolvida por `parceiro_id`, `conta["casa"]` **já é** a grafia canônica daquela conta — passá-la por `_casa_display(_display_to_key(...))` (`main.py:2354`) só pode corromper, e foi exatamente o que aconteceu com a Jonbet. Gravar a casa **verbatim** nesse ramo fecha a classe inteira do defeito para toda casa que venha a ser registrada no `_CASA_DISPLAY` **depois** de já existirem contas. Mexe em caminho compartilhado por todas as casas → mudança própria, com harness e `pytest` antes. Hoje o mecanismo está de pé por sorte: **1 grafia quebrada em 57**, e só porque ninguém mais cadastrou conta antes do mapa.

**BetNacional — divergência de rótulo de data, NÃO medida (anotada de passagem na s248):** o
`formatTicketBNC` (`extensor/content.js:2164`) emite só `Data (colocação):`, enquanto
`CASA_BETNACIONAL §4` diz que a coluna Data é a **do evento** — e que o campo do Histórico já é
"evento / liquidação". Pode ser só nome infeliz da variável (`t.colocada`), ou pode ser o mesmo
defeito que a VaideBet levou a produção na s210 e que a Jonbet quase repetiu (lá as duas datas
divergem em 7 de 10 bilhetes). **Custo de medir: baixo** — comparar `t.colocada` com
`pernas[].inicio` (o bloco já emite como `Início:`) num lote real da casa. Não mexer antes de
medir: se o campo já for o do evento, "corrigir" quebraria o que funciona.

**Badminton (s245) — medido, não aplicado (aguarda decisão do Feca):**
- ~~2 bilhetes em `Outro`~~ **CORRIGIDO na própria sessão** (ids 125105/125106 → `Badminton`) via `scripts/corrigir_esporte_bilhete.py` (novo), que **reusa `atualizar_bilhete`** em vez de rodar UPDATE cru — assim a trilha em `correcoes` é gravada (2 linhas, `esporte: Outro → Badminton`) e a assinatura é reavaliada (não muda: `esporte` ∉ `_SIG_COLS`). Base do Feca: **19 → 21** bilhetes de badminton, P/L do tipster **Bad Milton** 169,67 → **547,17** (+377,50 = os dois W que estavam fora do esporte). Varredura por nome de atleta + faixa de mercado não achou **nenhum outro** badminton escondido em `Tênis`/`Outro` na base do Feca; os 18 "candidatos" que o filtro pegou são todos legítimos de **times** (eBasket, Rugby, Basquete) — a fronteira que a regra nova já declara.
- **Superbet não emite esporte/liga no texto do bilhete.** `formatTicket` (`extensor/content.js:911`) manda data, stake, odd, status e seleções; Betfair (`:1169`), Pinnacle (`:1335`), KTO, Betfast e BETesporte mandam `Esporte (casa) · Liga`. Fecharia o buraco de **todos** os esportes dessa casa, não só badminton — mas exige ler os nomes dos campos no JSON de `/user/{id}/tickets` (F12 → Network). Não há fixture de Superbet no harness.
- **Categoria (não esporte):** 5 bilhetes Betano de 10–11/06 com `Over 78.5 Pontos [dupla v dupla]` estão em `Player Props` — é total da **partida**, então `Pontos` (`MASTER_APOSTAS §6`); e 9 bilhetes em `Games` onde o mesmo §6 manda `Sets`.

**Achados de performance da s217 — medidos, não corrigidos (decisão do Feca, um por vez):**
- **`sims=10000` fixo mesmo com a base cheia.** As chamadas passam `10000` explícito, o que **atropela** a escala adaptativa que o `_calcPValueMCraw` tem por dentro (`n>10000 → 3000`). Com 30.851 linhas são ~308 milhões de iterações por cálculo, duas vezes. Fora da thread principal a tela não trava mais, mas o valor ainda leva **~1 minuto** para chegar (conferido no demo com 24.000: os cards ficaram girando bem depois do render). Cair para ~2.000 sims em base grande resolveria — **muda número exibido** (p95/p99 e p-value), então exige antes/depois medido na mesa e aval do Feca.
- **`/uso/tokens` responde 500** em produção (visto ao cronometrar as rotas do feed). Não investigado.
- **A casca `/app` carrega 3 iframes e os 3 puxam `/dashboard/data`** — `/inicio`, `/` (Extração) e `/dashboard/`, cada um montando o feed inteiro no servidor (3 × 11,6 MB por abertura). Um cache compartilhado entre os frames (ou o feed servido uma vez pela casca) cortaria 2/3 do trabalho.
- **Deep-link a frio monta a tela vazia.** Abrir `/app#dash/metrics` sem cache local: a casca chama `showPage('metrics')` antes de o `buildHTML` existir, `_lastPage` já fica marcado, e o `loadData` termina **sem chamar `renderPage`** (`app.js:1183`) — o `showPage` seguinte volta cedo pelo `if(id===_lastPage&&sig===_lastPageSig)return`. A tela fica com os `—` do markup. **Pré-existente**, não veio da s217.

**Próximo passo (backlog vivo, um por vez):**
- **Backfill eSoccer — Feca FEITO (s234); resta o residual dos outros donos, se eles quiserem.** A auditoria da s234 varreu a Bet365 inteira e viu, fora do Feca: **Gabriel** ~14 bilhetes eSoccer como `Futebol` (tipsters próprios `Esoccer`/`LBB`) + **1 linha com grafia `Esoccer` na coluna esporte** (única da base; padroniza para `eSoccer` se mexer); Jonathan/William/LavaPessoal só têm eBasket-like (fora do escopo eSoccer). Método pronto e testado: script da s234 (`Backups/esoccer-backfill-feca-2026-08-02/`) — UPDATE por id auditado + perfil do tipster ganhando `eSoccer` nos `esportes` (senão o filtro duro do matcher mata as sugestões em silêncio, lição s221). `esporte`/`tipster` fora de `_SIG_COLS` → dedup intacta. Decisão do Feca (s234): **não tocar na base de outros donos sem eles pedirem**.
- **Matcher: "feudo empírico" — medido na s221, NÃO implementado.** O `Sugerir tipsters` é 100 % **declarativo**: lê só os perfis (casas · esportes · mercados · dica de stake) e **nunca** o histórico. Por isso as stakes **quebradas** (109,38 · 112,18 · 184,21…) ficam eternamente vazias — nenhum perfil declara quebrada na Bet365, e por stake elas são de todo mundo (M&M 284 · SóChutes 121 · SóTudo 31 · LBB 30). **O sinal que falta está na própria base:** no trio **casa · esporte · categoria**, um tipster domina. Medido na janela de 2.207 bilhetes que a tela **já carrega** (dá para computar no front, sem endpoint novo): `Bet365·Futebol·Gols → LBB 98 %` (226) · `Bet365·Futebol·Escanteios → SóTudo 82 %` (257) · `Bet365·eBasket·Pontos → Ctrl Alt Green 100 %` (219) · `Bet365·Tênis·ML → Robotenis 99 %` (168) · `Superbet·Múltiplos·Múltipla → Arrudex 92 %` (169) · `BETesporte·Futebol·Múltipla → Peixe 100 %`. Regra proposta: tipster **ativo** com ≥ 90 % e ≥ 15 bilhetes no trio leva; senão cai no matcher declarativo de hoje. **Cuidado que o próprio caso do 199 ensina:** o dono de um trio **muda com o tempo** (SóTudo → LBB entre maio e julho), então a janela precisa ser **recente**, não a base inteira — na base inteira `Bet365·Futebol·Gols` cai para 52 % de LBB e o critério não dispara. Fazer só depois de backtest com **holdout temporal** (treina no passado, mede no futuro), nunca in-sample.
- **Excluir conta: o caminho nunca rodou contra Postgres de verdade** (s219). Os 7 casos de `test_excluir_parceiro.py` usam conn simulado, como o resto da suíte. Três coisas ficaram sem prova real: o `DELETE ... RETURNING to_jsonb(b.*)`, o cast `::jsonb` do snapshot no INSERT da lixeira, e o corpo do `DELETE` via HTTP. **Teste barato, fazer antes de excluir qualquer conta com histórico:** criar conta descartável, capturar 1 ou 2 bilhetes, excluir pelo modal, e rodar `python scripts/restaurar_conta_lixeira.py` para ver se a linha aparece com a contagem certa. Depois `--aplicar` num id e conferir se as apostas voltam. A forma travada seria um caso em `tests/test_repository_db.py` (roda no CI com `TEST_DATABASE_URL`, nunca em prod).
- **Betfast: rodar a captura pela EXTENSÃO** (s211). A API já foi validada ao vivo (varredura do teto: 32 de 32, ver acima), mas o robô em si nunca rodou: recarregar a extensão, **Ctrl+Shift+R** na aba, capturar e conferir contagem/datas/odds/código no dashboard. **O gatilho do teto continua sem exercício ao vivo** — a conta que loga no navegador tem 32 bilhetes e não chega nas 50; para ver o toast *"a captura foi além do teto"* seria preciso rodar na conta `fecanario`.
- **Pinnacle sem fixture no harness** (s201): a instrução foi corrigida (§6/§11 não afirmam mais que a exibida é autoritativa em `W`), mas a leitura da odd **nunca foi travada contra dado real** — só Betfair, KTO e Tivo têm caso. Com o JSON do `POST /member-service/v2/wager-filter` dá para criar `fixtures/pinnacle.*.json` + `casos/pinnacle.mjs` e medir de fato o quanto a exibida diverge de `Retorno ÷ Stake`. O banco não serve para isso: guarda stake e odd, nunca o retorno.
- ~~**bet365 sem caso no harness** (s202)~~ **FEITO na s244** — `casos/bet365.mjs` trava a guarda `b3Emissivel`, o merge summary+confirmation, o corte do bloco KYC, a odd fracionária e o buraco da data. Harness passou de 7 para 8 casos (154 bilhetes).
- **Bet365 — data em bet builder de MESMO JOGO: DECISÃO DO FECA, medido e não corrigido** (s244). O `confirmation` desse tipo de bilhete vem com `TP=00010101000000` (sem kickoff) → `_dataFimB3` devolve vazio → o bloco sai **sem linha de data mesmo com o detalhe OK** → o backend cai na data de referência (= hoje). Provado por execução contra a fixture real, não deduzido. **O dado existe e está sem uso:** `da` (do confirmation, `DA=20260722233620`) e `tp` (do summary), ambos data de **colocação**. Usá-los contraria `CASA_BET365 §4`, que decide *"colocação nunca"* (cadeia `evento → informada → Brasília-hoje`) — logo é **mudança de regra**, não conserto de bug, e precisa da sua aprovação + atualização do §4 na mesma sessão. **CONFIRMADO na base, no fim da própria s244** (a 1ª medição tinha dado inconclusiva: 41,5% dos multi-seleção contra 24,1% dos simples, com captura diária confundindo o sinal — aquele número foi **descartado**, não use). O marcador `' // '` era grosseiro: mistura múltipla **entre jogos**, que tem kickoff, com bet builder de **mesmo jogo**, que não tem. Marcador exato: **2+ trechos e todos os confrontos `[A v B]` IGUAIS**. Com **controle dentro do mesmo lote** de captura (elimina o confundidor), `data == dia da captura`: **mesmo jogo 14/27 = 51,9%** · entre jogos 16/71 = 22,5% · simples 58/420 = 13,8%. O caso sem explicação inocente: no lote de 04/08 18:53:31, **0 de 20** simples eram do dia da captura e **2 de 2** bet builders de mesmo jogo eram. **Escala: pequena** — 34 bilhetes dessa classe na Bet365 do Feca desde 01/07, ~18 com data suspeita; hoje foram **6** (Internacional×Corinthians, Palmeiras×Fortaleza, Athletico-PR×Vitória, Juventude×Atlético-MG, Sonego×Griekspoor, Yu Chen Han×A Yeon Yoo). Dos 218 bilhetes Bet365 que entraram hoje, 84 têm data de hoje e **só esses 6** são da classe defeituosa; os outros 78 derivam do kickoff real. **O Feca viu o sintoma e o reportou** ("apostas de dias anteriores aparecendo como de hoje"), foi medido, ele leu o resultado e **decidiu não corrigir agora**. **Enquadramento para a decisão, quando ela vier:** a regra atual não escolhe entre colocação e data do evento — escolhe entre **colocação** (data do bilhete, erra por horas) e **o dia em que a captura rodou** (data da máquina, erra por semanas no modo Período). Proposta: cadeia `kickoff das pernas → colocação → data informada`, aplicando colocação **só** quando a API não dá kickoff, com o `CASA_BET365 §4` atualizado na mesma mudança. `casos/bet365.mjs` trava o estado atual **de propósito**: mexer nisso falha o gate e obriga a decisão consciente.
- ~~**Limpeza dos 139 da `marloncezar01`**~~ **APLICADA pelo Feca na s244** (o classificador barra escrita destrutiva em prod, então ele rodou via `!`). Verificado por fora, não pela saída do script: snapshot `removidos-Feca-20260804T194239Z.json` com **139 linhas de 22 colunas**, todas sem código e todas do parceiro certo, somando **exatos R$ 3.721,45** — o mesmo valor medido antes do DELETE. Base: **4447 → 4308** · sem-código **3586 → 3447** · `data = hoje` **168 → 29** (os 29 legítimos, que têm código) · lote do dia **206 → 67**, com código, data correta e confronto na descrição. Todos os deltas = 139.
- **Jonathan: 10 bilhetes Bet365 sem código em 04/08 — SUSPEITO, não confirmado** (s244). Mesma casa e mesmo dia do defeito do Feca, conta `edy-luc@hotmail.com [Gustavo]`, 10 de 10 sem código. **Não dá para separar robô de print pelo dado**: a coluna `origem` é `extracao` nos dois caminhos. Se for o mesmo defeito, o mesmo script limpa (trocando `--dono`/`--parceiro`); antes disso, perguntar ao Jonathan se ele capturou por print. **Não mexer na base de outro dono sem ele pedir** (mesma decisão da s234).
- **`renomear_parceiro` não recalcula a assinatura** (s198): `parceiro` entra no hash, então toda conta renomeada duplica o histórico na próxima captura.
- **41 apostas com odd truncada em reticências** (`2.50001664442...`): 22 Bet365, 6 Novibet, 6 Bolsa, 4 Betfair, 3 Esportiva Bet. A instrução proíbe reticências e uma odd assim não converte para número — mexe em P/L, não é cosmético.
- Preencher pendências das casas existentes assim que amostras reais chegarem (ver lista acima).
- **Solto (cosmético):** favicon da KTO aponta para `kto.com`; o domínio real é `kto.bet.br`. Corrigir nos 3 mapas (`extensor/popup.js`, `app/static/index.html`, `app/static/dash/assets/js/data.js` + `inicio.html`).
- **Tela "Em Aberto" fora do material de venda** (s215): `scripts/demo/capturar.mjs` não captura a tela nova — o servidor de demonstração já a serve, falta só decidir se ela entra no showcase (e a numeração dos arquivos existentes muda). **Decisão do Feca: entra, mas só depois de a tela estar finalizada.**
- **Material de venda: 4 correções abertas nas capturas** (s214). O pipeline funciona (`scripts/demo/`: perfil → base fictícia → servidor → `capturar.mjs`) e as 8 telas saíram, mas quatro coisas travam o uso na landing. **(1) Contas irreais: o mock devolve 1.830 contas, o real são 102** — `servidor_demo.py:_parceiros()` deriva conta de cada par (parceiro, casa) visto no feed, então cada pessoa nasce com ~18 contas. **E a tela repete essas 1.830 embaixo de cada uma das 29 casas, somando 53.070** — essa segunda multiplicação foi medida, não diagnosticada; conferir se é forma do payload antes de mexer no front. **(2) `Diagnóstico de Risco` sai em "calculando…"**: é Monte Carlo de 10.000 simulações sobre 24 mil apostas e o screenshot dispara antes de terminar — esperar o cálculo, não o relógio. É o painel que sustenta o argumento estatístico da página. **(3) Custo de contas e de tipsters em R$ 0**, então o "P/L Líquido" fica idêntico ao bruto e a tela perde justamente o recurso que diferencia — popular `/custos/store` no mock. **(4) `Nível de Solidez: Baixa`**, com MDD 90,88% e Recovery Factor 0,94×. É coerente com odd média 7,7, mas é a nossa própria régua reprovando a operação da demonstração. Subir o edge de 4,5% para ~8% leva o ROI a ~4% e a folga para faixa saudável, sem sair do plausível.
- **Landing do usuário final: reescrita aprovada, não feita** (s214). O Feca pediu duas frentes: **tirar detalhe de arquitetura** e **entrar com print real**. Sai da página a seção "Integramos o motor" (Kambi/BetConstruct/Altenar), a aba "API do motor" do hero (`Koef`, `WinAmount`), os pesos e cortes da Solidez, "27 categorias fechadas", os "94,5% de acerto de categoria" e a faixa de números do hero. Fica o que é confiança e não mecanismo: não pedimos senha, a extensão não aposta nem move saldo, e o bloco "o que o Sharpen não é". **A ordem importa: print primeiro, texto depois** — hoje a página se defende explicando o mecanismo porque não mostra nada. Fonte em `docs/marketing/landing-usuario-final.html`.
- **Em Aberto: relato do Gabriel SEM confirmação** (s215, aberto): funciona para o Feca (57 abertas, R$ 13.325, conferido na sessão logada em `www.sharpen.bet`), mas o Gabriel disse que não. **Hipótese principal: casca velha ainda gravada no navegador dele** — o `no-cache` só vale a partir da próxima ida ao servidor, não desfaz cache já gravado; `Ctrl+Shift+R` uma vez resolve (F5 normal pode não recarregar o iframe do dashboard). **Antes de mexer na tela, distinguir pelo que ele vê:** cards com título e corpo vazio, sem nenhum KPI = ainda é cache; os 4 KPIs em `R$ 0` / "nada em aberto" = não é bug, a base dele não tem aposta aberta (conferir de quem é a base pelo filtro "Operador"). O jeito de medir é ler quais scripts o navegador dele carregou (`[...frame.contentDocument.scripts].map(s=>s.src)`) — versão antiga ou tag ausente = cache.
- **Frente worldwide (nova, plano aprovado):** construir a Fase 1 do [`docs/PLANO_EXTRACAO_WORLDWIDE.md`](docs/PLANO_EXTRACAO_WORLDWIDE.md) (confidence da IA + guardrail de enum) quando o Feca quiser. Fase 0 já validada (zero-shot 94,5% de acerto de categoria). Meta: extração universal + cache aprendido → "+adicionar conta" em autosserviço.

### Bloqueado por ação humana (não é bilhete)

- ~~Login `ZoraEsports` 401~~ **RESOLVIDO na própria s238** — mas a lição fica: **conta nova deployada ANTES da env var de senha nasce com hash vazio na tabela `usuarios`, e o seed (`ON CONFLICT DO NOTHING`) nunca conserta** — colar a env var depois não basta (desde o Deploy B da s233 quem autentica é a TABELA via cache; a docstring do seed ainda descreve a Fase 1). Medido: `length(senha_hash)=0` na linha. Fix: UPDATE manual do hash (o Feca rodou o script via `!`; o classificador do Claude Code barra mexida direta em credencial de produção) → login 200 na primeira tentativa. **Ordem certa daqui em diante: env var no Railway PRIMEIRO, push da conta depois.**
- **Env vars de senha no Railway (s216, s218, s220) — estado NÃO instrumentado.** `SENHA_LAVAPESSOAL_HASH` **está no ar e o Lava entra** (confirmado pelo Feca em 31/07, s222). `SENHA_WILLIAMOLIVEIRA_HASH` e `SENHA_VINICIUSOLIVEIRA_HASH`: **não confirmadas** — podem já ter sido coladas; ninguém atualiza esta linha quando a ação acontece fora do repo.
  > ⚠️ **Esta linha é uma pendência declarada, não uma medição.** Não afirme a partir dela que um login está quebrado — foi exatamente o erro cometido na s222. Para saber de verdade: `POST /login` devolvendo **200** = hash certo; **401** = ausente ou diferente (429 é rate-limit, 500 é erro do app). Sem esse teste ou a palavra do Feca, escreva "não confirmado".

  Procedimento, se algum dia faltar mesmo: os hashes **não entram no git** — se perdidos, gerar de novo com bcrypt e colar na caixa de Variables do Railway (literal, 60 caracteres, sem espaço nas pontas; o `$` do `$2b$12$…` chega mutilado por qualquer shell que interpole variável).
- **`LavaPessoal`: 30 apostas com stake 0** (s222) — importadas, mas **invisíveis no dashboard** (`dashboard_rows` corta `stake <= 0`). 23 delas têm resultado. Aparecem na grade da **Extração** (`list_bilhetes` não filtra), então a correção é humana: preencher a stake lá e elas entram no P/L. Todas do tipster `Peixe`, abr–jul.
- **`LavaPessoal`: duas contas pré-existentes vazias** (s222) — `Bet365 | monster@2025 [Richard]` e `Betano | karlmarxrosa@aurainteligente.com [221193Cy*]`, criadas em 30/07, arquivadas, **zero** bilhetes. Não vieram do import e não foram tocadas. Se forem lixo de teste, apagar pelo botão Excluir do Painel de Contas (s219).

Quando chegar um bilhete novo: abrir o arquivo da casa correspondente, preencher a pendência, rodar o checklist do `CLAUDE.md` se envolver categoria nova.

---

## 6. Rodar / produção

**App em produção:** `https://sharpen.bet/` (www.sharpen.bet → Railway)

Para rodar localmente:
```
cd app
pip install -r requirements.txt
# .env na raiz do Planilhador com ANTHROPIC_API_KEY e DATABASE_URL
uvicorn main:app --reload
# Abrir http://localhost:8000
```

---

## 7. Workflow

- **Backup antes de editar** — sempre em `Planilhador/Backups/<nome-descritivo>/`. Nunca usar `FDC Capital/Backups/` (é compartilhada por outros projetos da empresa).
- Arquivos completos, nunca diffs parciais.
- Uma mudança por etapa aprovada.
- Atualizar este STATUS.md ao fim de cada etapa.
- Projeto tem git + GitHub (`flrcarvalho/sharpen`, renomeado de `extrator` na sessão 129). Deploy automático via Railway conectado ao GitHub — push dispara deploy.
